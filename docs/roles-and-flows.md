# Роли и пользовательские потоки

> **Версия проекта:** 0.136.0 (см. `VERSION` в корне)
> **Состояние на:** 11 мая 2026 (после релизов password recovery, n8n absence-alert, Document.Update ownership, teacher analytics scope filter, Assignments bounded context + UI, Returned transition flow, Student resubmit + read endpoints + UI, Curriculum module backend [v0.116.0+v0.117.0], Curriculum frontend list page [v0.118.0], Curriculum detail+edit+submit dialogs [v0.119.0], Curriculum admin approve queue + Approve/Reject dialogs [v0.120.0], MFA TOTP enrollment for system_admin [v0.124.0 — RFC 6238 self-implemented], CI lint cleanup [v0.124.1 — misspell + prettier patch], Login-flow MFA gating backend [v0.125.0 — intermediate token + verify-login endpoint], Login-flow MFA frontend integration [v0.125.2 — MFAVerifyLoginStep + LoginForm conditional render + status-aware 401/422 mapping + i18n × 4], Polish patch [v0.125.3 — isMFAChallengeActive helper extract + act() test wraps + 400-case docs], Templates filter teacher-own [v0.126.0 — DocumentType.MethodistOnly + CanAccessByRole + migration 033 + GetAllTemplates(role) scope filter; bonus fix UpdateTemplate handler key+role values], Wrong-key bug class sweep [v0.126.1 — schedule canModifySchedule + announcements isAdmin + users avatar Upload/Delete admin override], CI lint cleanup [v0.126.2 — misspell `behaviour` → `behavior` × 5 files], Methodist-only UI toggle [v0.126.3 — TemplateEditorDialog flag toggle + UpdateTemplate signature extension MethodistOnly *bool + i18n × 4], Pre-commit hook setup [v0.127.0 — .husky/pre-commit (AmE/BrE grep + golangci + prettier + eslint) closes cumulative cleanup-patch class], Section aggregate B1a foundation [v0.128.0 — Curriculum → Sections two-level hierarchy, ADR-1 Beta independent AR + ADR-2 lifecycle inheritance + ADR-3 optimistic locking; 5 CRUD endpoints + migration 034; backend foundation для bulk-edit РПД], DisciplineItem aggregate B1a Layer 2 [v0.128.1 — ControlForm typed VO + 5 CRUD endpoints + migration 035 + two-level cross-aggregate lookup chain; reviewer skipped с honest disclaimer], Review hardening patch [v0.128.2 — closes v0.128.1 retroactive review Tier 1 findings: ListBySection 404 guard + audit reason assertion backfill + shared withAuth test helper; reviewer round-1 SHIP mean 9.14 / min 9; B1a re-shaped к 5-release initiative ADR-9], Bulk-edit transactional endpoint [v0.128.3 — POST /api/sections/:sectionID/items/bulk с UnitOfWork (RepeatableRead) + collect-all 409 conflicts + cross-section guard + atomic creates+updates+deletes; DBTX refactor 3 PG repos для tx-bound reuse; ADRs 10-14 locked; reviewer round-2 SHIP 8.86/8 после Tier 1 fix-cycle (errcheck × 8 + dead sentinel ErrBulkSectionDeleted)], **Frontend bulk-edit table view UI [v0.128.4 — useReducer state machine (13 actions + 3 selectors) + BulkEditTable presentational + BulkEditPanel container с 409 conflict resolution flow per ADR-12 outside-tx refetch; mounted на curriculum/[id]/page.tsx с per-section panel; pickBulkEditErrorKey pure mapper для toast i18n; ControlForm typed enum × 4 i18n keys × 4 locales; reviewer round-2 SHIP 8.86/8 после fix-cycle (3 race conditions: SUBMIT_CONFLICT keep-flag + Cancel guard + SET_REFRESHED ghost-guard)]** — **B1a initiative полностью closed (5/5 layers shipped: Section + DisciplineItem + Review Hardening + Bulk-edit + Frontend), defence-ready минимум закрыт 100%, MFA полностью end-to-end UI, Templates filter полностью end-to-end, role-key bug class fully closed, lint cleanup-patch class closed, CI/CD Pipeline cleanup [v0.128.5], Dependabot sweep + race-fix backfill [v0.128.6], Frontend bulk-edit a11y + DOM hardening [v0.128.7], Critical gRPC + axios security cluster + T2-1 a11y closure [v0.128.8], Next.js + next-intl + npm audit transitive cleanup [v0.128.9]**)
> **Источники:** код (`internal/modules/auth/domain/`, `frontend/src/lib/auth/`, `frontend/src/config/navigation.ts`), GitHub issues, `.taskmaster/`, `CHANGELOG.md`

> **Изменения в 0.102.2:** Уточнена концепция личных vs глобальных настроек. Каждый пользователь любой роли (включая студента) может настраивать своё рабочее окружение: тему оформления и подключённые лично к нему каналы уведомлений (Telegram, email, WebPush). Глобальные настройки (SMTP-сервер, push-провайдер, brand системы, n8n workflows, интеграция с 1С) — остаются только у системного администратора.

> **Изменения в 0.106.0:** ResourceDocuments добавлен в PermissionMatrix как 5×4 таблица (5 ролей × 4 действия). До этого права на документы были разбросаны по отдельным проверкам в handler'ах; теперь — единая декларативная матрица в `auth/domain/permission.go`. Закрывает несоответствие между документированной матрицей в `roles-and-flows.md` и реально работающими проверками. Тесты pin парность ролей и действий, отказ для неизвестной роли — failure-closed.

> **Изменения в 0.107.0:** Logout endpoint + Redis token blacklist + JWTMiddlewareWithRevocation. До этого JWT жил до истечения срока даже после "выхода" — токен оставался валидным. Теперь `POST /api/auth/logout` помещает jti в Redis blacklist (TTL = remaining lifetime токена), `JWTMiddlewareWithRevocation` проверяет blacklist на каждом запросе. Закрывает security gap из аудита: «Logout не существует, токен невозможно отозвать». Без миграции — состояние blacklist живёт только в Redis, при перезапуске Redis остаются только активные сессии.

> **Изменения в 0.108.x:** v0.108.0 — полноценный flow восстановления пароля (request → verify → confirm) с anti-enumeration. v0.108.1 — алерты о пропусках студентов теперь отправляются и в n8n (раньше workflow JSON лежал, но не подключён). v0.108.2 — `Document.Update` теперь явно проверяет авторство; преподаватель не может редактировать чужие документы (доменное правило `Document.CanBeEditedBy` + 403). v0.108.3 — `/api/analytics/*` теперь применяет scope-фильтр «свои группы» для роли teacher: список групп выводится из `schedule_lessons + student_groups`, фильтр пушится в SQL (`WHERE group_name = ANY($N)`), запрос чужой группы или экспорт по чужим студентам возвращают 403/empty.
>
> **Изменения в 0.109.0:** Введён новый bounded context `assignments` (academic Tasks Context) — отдельно от существующего `tasks` модуля (project management / issue tracker). Миграция 029 добавила таблицы `assignments` и `submissions`. Endpoint `POST /api/assignments/:id/grades` (RequireNonStudent) принимает оценку: usecase `SaveGrade` загружает Assignment, проверяет авторство преподавателя через `Assignment.AuthorizeGrader` (только автор может grade — 403 при попытке grade чужого задания), валидирует Score VO против `MaxScore` (422 при выходе за границы), lazy-создаёт Submission если ещё нет, фиксирует переход pending→graded (повторный grade возвращает 409 ErrAlreadyGraded), пишет audit `assignment.graded`, отправляет уведомление студенту через `NotificationUseCase.Create` (best-effort: при ошибке отправки grade не откатывается, фиксируется отдельным audit `assignment.grade_notify_failed`).

> **Изменения в 0.110.0:** Полный read-side flow для assignments + frontend grading UI. Три новых backend endpoint'а: `GET /api/assignments` (список с фильтрами `subject` / `group_name`, пагинация, scope-aware: преподаватель видит только свои, methodist / academic_secretary / system_admin — все), `GET /api/assignments/:id` (single), `GET /api/assignments/:id/submissions` (список submissions с JOIN на `users.name` для рендера). Все три за `RequireNonStudent` + защита-в-глубину на уровне handler'а: `callerScopeFromContext` whitelist'ит четыре non-student роли явно (любая неизвестная роль → 401, не "fall-through к unrestricted"). Read-side caller-scope правило вынесено в domain как `Assignment.AuthorizeAccess(unrestricted, userID)` — единый источник истины для grade/get/list submissions. Use case `Assignment.NewSubmissionScore(value)` забрал cross-aggregate validation Score↔MaxScore из SaveGrade — domain leak закрыт; `Score.max` (мёртвое поле) удалён. Frontend: `/assignments` (list page с фильтрами) и `/assignments/[id]/submissions` (детали + inline grade form per submission row), валидация баллов на клиенте через `validateGrade` (NEGATIVE / OVER_MAX / NOT_INTEGER / NOT_A_NUMBER), TZ-стабильный парсер дат `parseLocalDate` (CLAUDE.md правило #9), статус-фильтр (pending / graded / returned), error mapping 409/422/403 → локализованные toast'ы. i18n × 4 (ru/en/fr/ar). Студенты redirect'ятся на `/forbidden` на клиенте + RequireNonStudent на сервере.

> **Изменения в 0.111.0:** Returned transition flow — учитель / методист / академический секретарь / системный администратор теперь может вернуть работу студента на доработку через `POST /api/assignments/:id/returns` с body `{student_id, reason}`. Domain метод `Submission.Return(reason, returnedBy, now)` принимает переход `pending → returned` ИЛИ `graded → returned` (повторный возврат отклоняется через `ErrAlreadyReturned` → 409); инварианты в domain: причина обязательна, ≤4096 символов, returnedBy > 0 (`ErrInvalidReturn` → 422). При переходе из `graded` все grade-поля очищаются на entity и в БД (исторический grade сохраняется в audit как `previous_grade` / `previous_feedback`). Миграция 030 добавила колонки `submissions.{return_reason, returned_by, returned_at}` + двусторонний CHECK constraint (`status='returned'` ↔ return triple non-NULL AND grade triple NULL — defense-in-depth поверх domain). Use case `ReturnSubmissionUseCase` симметричен `SaveGradeUseCase`: load → AuthorizeGrader → load-or-fresh submission → Return → Save → notify (best-effort) → audit. Три audit события: `assignment.returned` (с captured previous_grade когда был), `assignment.return_denied` (попытка не-автором), `assignment.return_notify_failed` (SMTP fail не откатывает return). Извлечён narrow port `usecases.AuditSink` — `SaveGradeUseCase` и `ReturnSubmissionUseCase` теперь зависят от интерфейса (DIP), а `*logging.AuditLogger` структурно его удовлетворяет (zero blast radius на DI). Frontend: `ReturnDialog` (Radix modal с textarea для причины, character counter 0/4096, error mapping 409/422/403/default → локализованные toast'ы), кнопка `Вернуть на доработку` в `SubmissionRow` для статусов `pending|graded` (скрыта при `returned`), отображение `return_reason` + `returned_at` (sky-styled блок, дата через `parseLocalDate`). `GradeForm` теперь disabled при любом не-pending статусе (re-grade returned работы возможен только после resubmit'а студента — flow вынесен в v0.112.0). i18n × 4: 16 новых ключей (`assignments.returnDialog.*`, `assignments.returnButton`, `submissionRow.returned*`, `gradeForm.returnedReadOnly`). Out of scope для 0.111.0: student-side resubmit (`returned → pending`) — отдельный minor v0.112.0 когда появится student UI.

> **Изменения в 0.112.0:** Student-side resubmit flow (`returned → pending`) — замыкает академический re-grading loop, открытый в 0.111.0. Студент теперь может пересдать возвращённую работу через `POST /api/assignments/:id/resubmit` (тело пустое: `:id` из path + студент = JWT subject = единственно возможный owner). Domain метод `Submission.Resubmit(now)` принимает только `status='returned'` (иные статусы → `ErrNotReturned` → 409); очищает return triple (`return_reason / returned_by / returned_at`) и flip'ает status в `pending`. Grade triple НЕ ресуррект'ится — Return уже его обнулил, Resubmit оставляет нулям. Доменный invariant ownership вынесен в `Submission.AuthorizeResubmitter(actorID)` + sentinel `ErrSubmissionOwnerOnly` (отдельный от `ErrAssignmentScopeForbidden`: teacher-side rule на Assignment vs student-side rule на Submission — конфлятить нельзя). Use case `ResubmitSubmissionUseCase`: load assignment (для teacherID) → load submission по `(assignmentID, studentID)` → AuthorizeResubmitter → capture previous_return_reason → Resubmit → Save → notify teacher (best-effort) → audit. Три audit события: `assignment.resubmitted` (с captured `previous_return_reason` — forensic mirror к `previous_grade` на return стороне), `assignment.resubmit_denied` (попытка не-owner — security forensic trail), `assignment.resubmit_notify_failed` (SMTP fail — observability, не откатывает resubmit). Routing: новая sibling-группа `studentAssignmentsGroup` под `protectedGroup.Group("/assignments")` с `RequireRole("student")` middleware (assignmentsGroup за `RequireNonStudent` — exact inverse, так что endpoint ушёл в отдельную группу). Защита-в-глубину: `studentIDFromContext` whitelist'ит ТОЛЬКО роль student на handler-уровне. Failure-closed DI: `NewResubmitHandler` panics при nil usecase. Никакой новой миграции — схема 030 уже допускает `(status='pending', returnTriple=null)`. `logAudit` дублированный код выделен в package-private helper `emitAudit` (триггер «extract on N=3» от reviewer'а v0.111.0). Out of scope для 0.112.0: **frontend resubmit UI** — student dashboard в проекте ещё не существует (все assignments-страницы выгоняют студентов через `if user.role === 'student' router.replace('/forbidden')`), кнопка «Пересдать» отложена до отдельной student-dashboard initiative. Endpoint лежит готовым seam'ом для будущего UI.

> **Изменения в 0.136.0:** Branding admin backend (Phase 5 #4 final, half 1 of 2 — backend persistence + API; frontend split к v0.137.0). True greenfield module — впервые в Phase 5 first **write** feature после 5 read-only admin observability releases. **Backend** (`internal/modules/branding/`, новый модуль): `BrandSettings` aggregate singleton с 6 editable fields (app_name 1-100 chars / tagline ≤200 / logo_url + favicon_url http/https whitelist / primary_color + secondary_color hex `#RRGGBB|#RGB`) + 4 typed domain error sentinels (`ErrInvalidAppName`/`ErrInvalidTagline`/`ErrInvalidColor`/`ErrInvalidURL`). URL scheme whitelist (http/https only) blocks javascript:/data:/file:/ftp: from sneaking onto login page img tag — defense-in-depth перед React renderer. Migration 037 — singleton table `brand_settings` с `CHECK (id = 1)` + length CHECKs + DEFAULT seed row idempotent `ON CONFLICT DO NOTHING`. `BrandSettingsRepositoryPG` (`Get`/`Update`, sqlmock-pinned WithArgs × 7 columns including `updated_at`). Use cases: `GetBrandingUseCase` thin delegate; `UpdateBrandingUseCase` композит domain validation + repo write + audit emit с `Clock` port injection + `AuditSink` narrow port (DIP). **Endpoints**: `GET + PUT /api/admin/branding` (system_admin only, domain errors → 422 с typed codes INVALID_APP_NAME/etc.); `GET /api/public/branding` (no auth, rate-limited через existing `publicRateLimiter`, shared DTO byte-for-byte). Audit emit `brand.updated` на successful PUT с full resulting snapshot + actor_user_id из JWT context. **Wire** `cmd/server/main.go` — modulе construction near existing `*sql.DB`/`AuditLogger` setup; public route mount в independent `router.Group` block (uncoupled from sharing-feature flag). **20 backend tests** (11 domain table-driven 4 axes / 4 sqlmock / 5 admin handler integration / 2 public + nil-panic) all green; backend 110 packages green / 0 lint. **Reviewer round-1 SHIP** mean **9.0** / min **8** single-pass — 3-й consecutive single-pass SHIP (v0.134.0 9.13/9 + v0.135.0 9.29/9). Per-axis: TDD 9 / DDD 9 / CA 8 / Security 9 / Testing 9 / Code Quality 8 / Migration 9. **Tier 1 absorbed** в release commit: dead `_ = strings.TrimSpace` placeholder removal + unused `strings` import drop + `BrandSettingsDTO.UpdatedAt` docstring justifying public surface (stable cache key, не secret). **Tier 2 deferred к v0.137.0 backend follow-up**: ADR-7 route extraction `RegisterBrandingRoutes(...)` mirror к v0.133.0 `users.RegisterUserRoutes` (inlined в main.go в v0.136.0); `UpdateBrandingInput` struct для 7-positional shape. **71-й релиз** (43 minor + 28 patch). Phase 5 #4 final **backend done**; frontend (`/admin/branding` page + login page consumption + `useBranding` SWR hook + Nav entry) closes Phase 5 #4 в v0.137.0.

> **Изменения в 0.135.0:** Admin Composio config view (Phase 5 #5 partial — Composio observability slot). Read-only admin surface at `/admin/composio` over the runtime Composio integration state (API key + entity ID + MCP config ID). Composio infrastructure was already wired before this release (`internal/shared/infrastructure/composio/client.go` + 4 consuming services в `notifications/application/services/`); admins had no visibility into wiring status без `docker logs`. This release closes the gap — **5-й** последовательный admin observability surface в codified template (после audit-logs, backups, sentry, integrations). **Backend** (`internal/shared/admin/composio/`, new package): `GET /api/admin/composio/config` returns `{configured, api_key_configured, entity_id_set, mcp_config_id_set}` — только booleans, никаких raw values. API key — signing secret; entity ID + MCP config ID — opaque platform identifiers (per VAPID privacy precedent). Aggregate `configured` true только когда все 3 env vars non-empty; per-field booleans позволяют админу видеть какой именно field missing в partial-state runtime. `EnvComposioProbe` reads `COMPOSIO_API_KEY` + `COMPOSIO_ENTITY_ID` + `COMPOSIO_MCP_CONFIG_ID` directly (env-direct mirror к VAPID). Route-level role gate at `adminGroup` (mirror precedent). 5 handler tests + 2 table-driven 8-case (`ProbeResult.AllConfigured` + `EnvComposioProbe` matrix). **Frontend** `/admin/composio`: single status card (mirror к `/admin/sentry` single-card layout — Composio = 1 service). Badge flips on aggregate `configured`; dl rows render `fields.set` / `fields.unset` markers per field. `useComposioConfig` SWR hook + `ComposioConfig` type mirror backend exactly. Nav entry `composio` × 4 locales (Bot icon, system_admin allowlist). **i18n × 4** (ru/en/fr/ar): `adminComposio.*` namespace 11 keys + `nav.composio` (brand name identical). **2 RED→GREEN pairs** (4 commits) + 1 Tier 2 absorb + release. **Reviewer round-1 SHIP** mean **9.29** / min **9** single-pass (TDD 9 / DDD 9 / CA 10 / Security 10 / Tests 9 / Code Quality 9 / i18n 9). Tier 2 absorbed: `initialisation` → `initialization` × 2 BrE→AmE comment fix per `feedback_misspell_be_to_ae`. 70-й релиз проекта (42 minor + 28 patch). Backend `109` packages green / 0 lint. Frontend `3223` tests passing across `213` suites (44 new). **Phase 5 #5 partial closed** (Composio observability done; SetReminder = cross-module write semantics, defer to Phase 5 #5 final). **Pattern locked**: 5 sequential admin observability surfaces codify the template для new admin surfaces. **Branding** (Phase 5 #4 final) + **SetReminder** (Phase 5 #5 final) + **#210 DIP** + **#196 coverage** + **B3 Extracurricular** + **v1.0.0 Final** = remaining work before tag.

> **Изменения в 0.134.0:** Admin integrations config view (Phase 5 #4 partial — VAPID + n8n bundle). Read-only admin surface at `/admin/integrations` over the runtime WebPush (VAPID) + n8n workflow automation configuration. **Backend** (`internal/shared/admin/integrations/`, new package): `GET /api/admin/integrations/config` returns combined `{vapid: {configured, public_key, subject}, n8n: {enabled, webhook_url}}`. VAPID private key never leaves the server — only its presence as boolean (DSN-style pattern from v0.133.0 `/admin/sentry`). Public key surfaces (browser receives it via `/push/public-key` anyway); n8n WebhookURL is non-secret operational URL. `EnvVAPIDProbe` requires all 3 env vars (`VAPID_PUBLIC_KEY` + `VAPID_PRIVATE_KEY` + `VAPID_SUBJECT`) non-empty — table-driven test across 8 sub-cases. **Frontend** `/admin/integrations`: two status cards (VAPIDCard + N8NCard) stack on mobile / side-by-side md+. Each card renders configured/enabled badge + operational fields. `useIntegrationsConfig` SWR hook + `IntegrationsConfig` type. Nav entry `integrations` × 4 locales (Plug icon, system_admin allowlist). **i18n × 4** (ru/en/fr/ar): `adminIntegrations.*` namespace 13 keys + `nav.integrations`. **2 RED→GREEN pairs** (4 commits) + release. **Reviewer round-1 SHIP** mean **9.13** / min **9** single-pass (TDD 9 / DDD 9 / CA 9 / Security 10 / i18n 9 / Tests 10 / Cohesion 9 / Mirror к v0.133.0 10). Zero Tier 0/1/2 findings; 3 Tier 3 cosmetic items deferred (AR translation of "Subject", hook unit-test, EnvVAPIDProbe relocation). 69-й релиз проекта (41 minor + 28 patch). Backend `108` packages green / 0 lint. Frontend `22` new tests passing. **Phase 5 #4 partial closed** (VAPID + n8n done; branding = greenfield, отложен к v0.135.0 separate session — нет ни domain, ни DB, ни config); **Phase 5 #5 (Composio + SetReminder) remains** before Phase 6 closure.

> **Изменения в 0.133.0:** Admin Sentry config + admin user management (Phase 5 #3) + TIER 0 security gap closure. Two new admin surfaces ship behind `system_admin` role guard: `/admin/sentry` (read-only view of the runtime Sentry integration — `DSNConfigured` boolean, environment, release, traces sample rate, tracing enabled) and `/admin/users` (filterable list + per-row Radix dialogs for change-role / change-status / delete operations wrapping the existing `/api/users` CRUD). **Security gap closure (TIER 0)**: prior to v0.133.0 `protectedGroup.Group("/users")` had no role guard, so any authenticated user could `PUT /api/users/:id/role`, `PUT /:id/status`, `DELETE /:id`, or invoke `/bulk/*`. The new `users.RegisterUserRoutes` helper splits the group into a permissive subgroup (any authenticated caller — `List`, `GetByID`, `by-department`, `by-position`, `GET avatar`, plus `UpdateProfile` and avatar `Upload/Delete` carrying the pre-v0.133.0 self-edit behavior forward) and an admin-write subgroup gated by `RequireRole(system_admin)` (`/role`, `/status`, `DELETE`, `/bulk/*`). The dead `adminGroup.GET("/users", stub)` placeholder removed. **Backend**: `internal/shared/admin/sentry/` — `Config` DTO (DSN exposed as boolean only — value never leaves the server), `AdminSentryUseCase` with injectable `DSNProbe` for deterministic test coverage, `AdminSentryHandler.GetConfig` mounted under `adminGroup`. `internal/modules/users/interfaces/http/routes/routes.go` — new package extracting the users-module routing out of `cmd/server/main.go`. **Frontend**: `/admin/sentry` mirrors `/admin/backups` read-only status-card pattern. `/admin/users` provides search/role/status filters + pagination (20/page) + role/status badges + three Radix Dialog wrappers (change role / change status / delete) wired through three thin mutation hooks (`useUpdateUserRole`, `useUpdateUserStatus`, `useDeleteUser`) that surface `isLoading` + `error` for dialog feedback. Navigation entries `sentry` + `adminUsers` × 4 locales added to `adminGroup`. **i18n × 4 locales** (ru/en/fr/ar): `adminSentry.*` (12 keys) + `adminUsers.*` (70+ keys covering filters, columns, actions, roleOptions, statusOptions, pagination, three dialog subtrees with `{name}` ICU placeholders) + `nav.sentry` + `nav.adminUsers`. **5 RED→GREEN pairs** (10 work commits: security guard split + Sentry backend + Sentry frontend + users list + users dialogs) + 1 chore (nav + stub removal) + 1 Tier 1+2 absorb + 1 release. **Reviewer round-1 SHIP mean 8.94 / min 8** (TDD 9 / DDD 9 / CA 9 / Security 8 / i18n 10 / Tests 9 / Cohesion 8 / Plan adherence 9). Tier 1 absorbed: self-edit regression on `UpdateProfile` + avatar Upload/Delete — those three moved back to the permissive subgroup so non-admin users keep editing their own profile and avatar (the avatar handler already enforces a self-or-admin override; UpdateProfile carries the pre-v0.133.0 permissive state forward unchanged; hardening to a usecase-level ownership check deferred to a follow-up patch). Tier 2 absorbed: nav.adminUsers label disambiguated (`Управление пользователями` / `User management` / `Gestion des utilisateurs` / `إدارة المستخدمين`) to avoid collision with `nav.users`; `ROLE_VALUES` / `STATUS_VALUES` constant duplication removed in page.tsx. Tier 3 deferred к v0.133.x backlog: dialog component extraction (585-LOC page.tsx → components/admin/users/), empty `catch {}` error surfacing in dialogs, DeleteUser self-delete guard (pre-existing), `cfg.Sentry` struct refactor (only if second consumer emerges). **68-й релиз проекта (40 minor + 28 patch)**. Backend `108` packages green / 0 lint. Frontend `3179` tests passing across `209` suites. **Phase 5 #3 closed; #4-#5 (VAPID + branding + n8n, Composio + SetReminder) remain before Phase 6 closure.**

> **Изменения в 0.132.0:** Backup admin observability (Phase 5 #2). Read-only admin surface at `/admin/backups` over the existing `/backup/` sidecar container (already shipped in earlier commits — pg_dump + MinIO tarball + age/GPG encryption + Prometheus textfile metrics + Telegram/Email/Webhook notifications + offsite S3 sync + retention GC). The new in-app surface gives `system_admin` a browser view of what the sidecar produced without SSH-ing into the host. **Backend**: `GET /api/admin/backups` returns combined file listing + metrics in one round-trip; `GET /api/admin/backups/:type/:name/download` streams a vetted file with `Content-Disposition: attachment` and emits `backup.downloaded` audit (filename/size/type/actor_user_id). Two narrow ports satisfied by `FileReader` (filesystem walk + anchored whitelist regex `postgres_YYYYMMDD_HHMMSS.sql.gz[.age|.gpg]` / `minio_YYYYMMDD_HHMMSS.tar.gz[.age|.gpg]` + encryption-suffix classification) and `MetricsReader` (Prometheus textfile parser tolerant к missing blocks and missing file). Path-traversal defence: anchored regex + `filepath.Clean + hasPrefix(cleanRoot+sep)` belt-and-braces. New `BackupConfig` (`BACKUP_FILES_DIR`/`BACKUP_METRICS_DIR`) surfaced via env. **Frontend**: `/admin/backups` page mirrors `/admin/audit-logs` shape — role guard, AppLayout, metrics tile (postgres + minio cards с OK/failed/no-data status pill + 2-col dl), optional remote-sync banner, file table (name/type/size/mtime/encryption badge/download button). Download uses `window.open(url + ?token=jwt, '_blank')` per documents page precedent — `<a download>` cannot carry `Authorization` header, so backend's middleware fallback (auth_middleware.go:23-40 — documented для file downloads) accepts the query-param token. i18n × 4 (ru/en/fr/ar) с ~36 keys per locale; JSON-load parity test pins every key. **Infrastructure**: `compose.yml` mounts the sidecar's `backup_data` and `backup_metrics` named volumes read-only into the backend service — sidecar owns writes, backend only reads. **Out of scope (NOT implemented)**: no trigger backup from UI (sidecar API extension required — v0.132.x backlog); no restore (wipe DB blast radius warrants focused review in separate release); no delete (sidecar retention GC handles это); no encryption key management UI. **4 RED→GREEN pairs** (8 work commits) + 1 Tier 1+2 reviewer absorb + 1 release. Reviewer round-1 verdict mean 8.29 / min 6 (Security 6) — FIX-CYCLE — single Tier 1 finding: the initial implementation used `<a href download>` which issued a no-auth GET (would 401 in production); fix mirrors documents page `?token=` pattern. Tier 2 absorbed same-cycle: audit resource string `backup_admin` → `backup` (codebase convention uses noun being acted on). 67-й релиз проекта (39 minor + 28 patch). Backend 108 packages green / 0 lint в new package. Frontend 148 tests passing (7 page + 141 i18n parity). **Phase 5 #2 closed; #3-#5 (admin/users + Sentry, VAPID + branding + n8n, Composio + SetReminder) remain before Phase 6 closure.**

> **Изменения в 0.131.1:** Audit logs coverage gaps (Phase 5 #1, третий и финальный слот 3-release initiative). После v0.130.0 backend persistence + v0.131.0 read API/UI закрыты два модуля, которые не эмитили audit events: messaging mutations и integration 1C sync/conflict resolution. Forensic timeline теперь covers ВСЕ операции, которые мутируют state of record. **Messaging emissions** (5 methods): `CreateDirectConversation`/`CreateGroupConversation` → `conversation.created`, `UpdateConversation` → `conversation.updated`, `SendMessage` → `message.sent`, `DeleteMessage` → `message.deleted`. Resource split `conversation`/`message` — read API filter targets streams отдельно. Денайл-пути (existing direct shortcut, non-admin update, non-participant send, non-author delete) emit nothing — forensic false-positive guard. **Integration emissions**: `SyncUseCase.StartSync` lifecycle pair `integration.sync_started` then `integration.sync_completed`/`integration.sync_failed` (invariant: every attempt leaves trail); `CancelSync` → `integration.sync_canceled`; `ConflictUseCase.Resolve` → `integration.conflict_resolved`; `ConflictUseCase.BulkResolve` → `integration.conflict_bulk_resolved` (один summary event с `conflict_count`). `emitIntegrationAudit` helper accepts `actorID == 0` для cron-triggered sync. **Architecture**: `AuditSink` narrow port в consumer package (DIP), `WithAuditSink` chainable setter (backward-compat с 6-positional constructor); `Module.WithAuditSink` для integration chains sink в обе use cases. **2 RED→GREEN pairs** (4 commits) + 1 Tier 2 absorb + 1 release. 21 sub-case total (11 messaging + 10 integration: success + denial + nil-sink). **Reviewer round-1 SHIP mean 8.86 / min 8** single-pass. 3 Tier 2 absorbed: zero-actor guard в messaging helper, sync emit order swap (persist before emit), test rename. Tag `v0.131.1` `<release-commit>`. Backend 108 packages green / 0 lint. Frontend untouched. **Audit-logs initiative closed 3/3**.

> **Изменения в 0.131.0:** Audit logs read API + admin UI (Phase 5 #1, второй из 3-release initiative). Замыкает observability loop из v0.130.0: persisted события теперь видны system_admin'у через web UI. **Backend**: `GET /api/admin/audit-logs?action=&resource=&user_id=&from=&to=&limit=&offset=` под `adminGroup` с `RequireRole(system_admin)`. `AuditLogRepositoryPG.List(ctx, filter)` — sentinel-arg WHERE clause mirror к curriculum/section list pattern; COUNT и SELECT share same predicate (correct pagination past tail); ORDER BY `created_at DESC, id DESC` pairs с existing `idx_audit_logs_created_at`; half-open `[from, to)` range avoids midnight double-count. Narrow ports `AuditLogReader` / `AuditLogFilter` / `AuditLogListResult` объявлены рядом с writer в `internal/shared/infrastructure/logging/audit_log.go`. **Новый shared subpackage** `internal/shared/admin/auditlog/` (placeholder для будущих cross-cutting admin features — backup/restore, etc.): thin use case (clamp Default=50/Max=200 через `ClampLimit` helper, validate half-open time range с `ErrInvalidTimeRange` sentinel) + HTTP handler (parse query, map to `response.List`, 400 on parse error, generic 500 on reader error). **Frontend**: `/admin/audit-logs` page — filter bar (action/resource/actor user_id/RFC3339 datetime-local from+to/reset) + table (timestamp/action/resource/actor/IP/correlation/expandable JSON fields) + pagination (prev disabled at offset=0, next disabled when page≥total_pages). `useAuditLogs` SWR hook с custom envelope-lifting fetcher (data + meta.pagination → flat `{items, pagination}`). Nav entry под `adminGroup` (FileText icon, system_admin allowlist). i18n × 4 (ru/en/fr/ar) — `nav.auditLogs` + `adminAuditLogs.*` (~21 ключей × 4 = 84 entries); JSON-load parity test reads raw message files. **4 RED→GREEN pairs** (8 work commits) + 1 Tier 2 absorb + 1 release. **Reviewer round-1 SHIP mean 9.0 / min 8.5** (TDD 9 / DDD 9 / CA 9 / Security 9 / Testing 9 / Code Quality 8.5 / i18n 9.5). Tier 2 absorbed: `ClampLimit` helper extraction (handler/usecase clamp drift fix), dead `filters.limit` + `filters.apply` i18n keys removal (×4 locales). Backend 108 packages green / 0 lint. Frontend 104 tests passing (page + hook + i18n parity).

> **Изменения в 0.130.0:** Audit logs persistence (Phase 5 #1, backend-only). Первый из 3-release initiative (plan в `docs/plans/2026-05-11-audit-logs.md`); v0.131.0 добавит read API + `/admin/audit-logs` UI, v0.131.1 — coverage gaps в messaging+integration модулях. До v0.130.0 `AuditLogger.LogAuditEvent` писал только stdout structured logs — теперь дополнительно персистит каждое событие в новую таблицу `audit_logs` (миграция 036: id/created_at/action/resource/actor_user_id/actor_ip/correlation_id/fields JSONB + 5 индексов + 2 CHECK). **ADR-2** sync write через `db.ExecContext` напрямую (не через `*sql.Tx`) — независим от любой business transaction, так что denied/failed бизнес-операции тоже аудитятся. **ADR-3** writer failure логируется ERROR-level (action+resource+cause) и НЕ пропагируется к caller'у — fire-and-forget. **ADR-6** `actor_user_id` БЕЗ FK на `users(id)` — audit log переживает user deletion. **ADR-7** `WithRepository(writer)` setter pattern — existing `NewAuditLogger(logger)` call sites backwards-compatible (log-only когда no writer). **Tier 1 critical (closed)**: reviewer round-1 поймал что production middlewares писали ctx values через unexported `middleware.contextKey` type, а AuditLogger читал через `logging.ContextKey` (другой тип) — все 3 actor поля landed NULL в 100% production rows. Fix: `RequestIDMiddleware` + `RequestContextMiddleware` + `JWTMiddleware` теперь promote actor keys в typed context рядом с существующими unexported keys (backwards-compat preserved). Pinned RED tests через real `gin.Engine`, не self-confirming `context.WithValue` mirror. **3 RED→GREEN pairs** + 1 Tier 2 absorb commit (T2-1 docstring claim correction + T2-2 AmE leaks optimises/serialisable + T2-3 extractor type-mismatch tables + T2-4 fallback log-line `captureStdout` assertion + T3-1 nil-fields guard в Write) + 1 release commit. Reviewer round-1 FIX-CYCLE mean 7.0 / min 4; round-2 **SHIP mean 8.71 / min 8** после всех fixes. Backend 108 packages green / 0 lint issues. Frontend untouched.

> **Изменения в 0.129.1:** Annual report Clean Architecture polish — backend-only patch closing carry-forward TODOs от v0.129.0 reviewer round (без user-visible изменений). **Narrow port extraction**: новый `DocumentActivityReaderPG` (`internal/modules/documents/infrastructure/persistence/`) — read-only adapter, экспонирующий единственный метод `AggregateActivityByType`, делегирующий к package-private SQL helper. `cmd/server/main.go` annual report wiring переключён с fresh `NewDocumentRepositoryPG(db)` на `NewDocumentActivityReaderPG(db)` — duplicate-handle smell (TODO line 511-518 из v0.129.0) устранён; cross-module orchestrator больше не зависит от полного `DocumentRepository`'s construction invariants. **Dead-method removal**: `AggregateActivityByType` удалён из interface `DocumentRepository` + impl `DocumentRepositoryPG` + 1 sqlmock test + 2 testify mocks — после рефакторинга единственный production consumer (annual report) идёт через narrow port; method был dead в production. DTO `DocumentActivityByTypeAgg` retained (consumed via narrow port). CLAUDE.md DDD-gate "никаких на будущее в domain" honoured. **Integration test** (closes v0.129.0 Tier 3.1): `annual_report_integration_test.go` exercises full chain через `httptest.NewServer` — gin router + real handler + real `AnnualReportUseCase` + fake aggregate repos + REAL `docxgen.Renderer` + `archive/zip.NewReader` validation. Asserts response парсится как valid OOXML zip, `word/document.xml` содержит every section header + every aggregate value, `report.annual_generated` audit event fires exactly once, escape-safe pipeline preserves XML metacharacters (`& < > "`) как escaped entities без leaking raw markup. **TDD**: 1 RED→GREEN pair (narrow reader) + 1 honest `test: backfill coverage` (integration) + 1 `refactor:` (dead method removal). **Reviewer round-1** FIX-CYCLE mean 8.67 / min 8 — 3 Tier 2 (`require.Error` без wrap-verification, escape-safe claim без metachar fixtures, dead method в domain interface); все absorbed в same release per `feedback_tier2_absorb_same_release`. **Reviewer round-2 SHIP** mean 9.0 / min 9.0 — matches v0.129.0 baseline. 5 коммитов + Tier 2 fix-cycle + 1 release.

> **Изменения в 0.129.0:** B4 Annual methodist report — cross-module read-only orchestrator, печатный DOCX-отчёт за календарный год для деканата. Methodist + system_admin only (`RequireRole`); academic_secretary excluded (observer, не decision-maker per ADR-6). Endpoint `GET /api/reports/annual?year=YYYY` streams DOCX bytes (`Content-Type: application/vnd.openxmlformats-officedocument.wordprocessingml.document` + `Content-Disposition: attachment; filename="annual_report_YYYY.docx"`). 4 section aggregates compiled per year: curricula по специальностям и статусам, распределение submissions по subject+status, sum часов (лекции/практика/лаб/самостоят) per curriculum, активность documents по type+status. **Pure-stdlib DOCX** (`archive/zip` + `encoding/xml.EscapeText` + `strings.ReplaceAll` per ADR-2) — zero supply-chain risk; `internal/shared/docx.Substitute` helper + `internal/modules/reports/annual/infrastructure/docxgen` template-as-code synthesizer (3-entry minimal OOXML package: `[Content_Types].xml` + `_rels/.rels` + `word/document.xml`). XML injection защита pinned тестом (`<script>` → `&lt;script&gt;`). Deterministic byte-identical output (zero ModTime на zip entries). Empty year renders 4 секции с маркером «Нет данных за период» per ADR-11 (НЕ 404). Aggregate row-type DTOs (`CurriculumYearSpecialtyAgg` / `AssignmentGradeDistributionAgg` / `DisciplineItemHoursAgg` / `DocumentActivityByTypeAgg`) live в producer-module `domain/repositories/` — отвергнут backward dep producer→consumer. 11 TDD pairs (24 commits) + Tier 2 reviewer cleanup. Backend: `internal/modules/reports/annual/{application,infrastructure,interfaces}/` новый module skeleton + 4 aggregate queries в существующих producer-modules (curriculum/assignments/documents). Frontend: `/reports/annual` page (year selector last 10 years + Download button с blob → object URL → anchor click) + role guard redirect to `/forbidden`; nav entry в analyticsGroup (FileCheck icon, methodist + system_admin). i18n × 4 (nav.annualReport + 6 reports.annual.* keys × ru/en/fr/ar) + JSON-load parity test против `useTranslations` mock illusion. Audit event `report.annual_generated` (forensic trail на personal-data aggregate access) on success only — НЕ fires при early failure. Reviewer round-1 SHIP mean 9.0 / min 9.0 (TDD 9 / DDD 9 / CA 9 / Security 9 / Testing 9 / Code Quality 9); Tier 2 fixes absorbed в same release (swagger annotations + call-order tracker + `docxBytes` shadow rename + DI smell TODO). Out of scope (deferred): integration test через `httptest` с реальным renderer (manual Word/LibreOffice verification per ADR risk #5); aggregate query caching (ADR risk #2); `AnnualReportDocumentReader` narrow port extraction (DI smell, TODO v0.130.x).

> **Изменения в 0.128.10:** Security — CodeQL sweep: closes first default-setup CodeQL scan (**2 errors + 32 warnings = 34 → 0**). **Critical fix**: 2 SQL injection `go/sql-injection` errors на `EventRepositoryPG.List` (`event_repository_pg.go:280`) и `DocumentRepositoryPG.List` (`document_repository_pg.go:279`) — user-controlled `filter.OrderBy` (from `?order_by=` query parameter via `form:"order_by"` DTO binding) interpolировался через `fmt.Sprintf` в ORDER BY clause без validation. Vector: blind SQL injection через ORDER BY side-channel (e.g., `?order_by=(CASE WHEN (SELECT password_hash FROM users WHERE id=1) LIKE 'a%' THEN id ELSE created_at END)` — leaks data row-by-row). Mitigation: whitelist maps `validEventOrderBy` (11 clauses) + `validDocumentOrderBy` (11 clauses) check `filter.OrderBy` против allowed set before any SQL execution; returns `repositories.ErrInvalidOrderBy` domain error on unknown values. Table-driven TDD pairs × 2 (5 attack vectors каждый — semicolon DDL / CASE blind / raw DROP / comment terminator / unknown column). **Hardening**: 32 CodeQL `actions/missing-workflow-permissions` warnings — declared minimal `permissions: contents: read` (+ `pull-requests: read` для pr-validation) на workflow level в 7 файлах (backend-ci / backup-test / database-ci / docs / frontend-ci / pr-validation / security); behaviour preserved — все existing steps read-only. `ci.yml` already had per-job blocks с v0.128.5 — unchanged. 5 commits: 2 TDD pairs (RED + GREEN × 2) + 1 workflow hardening. 103 Go packages + tsc + prettier + eslint clean.

> **Изменения в 0.128.9:** Security — Next.js + next-intl + npm audit fix transitive cleanup. Closes ~18 dependabot alerts (combined HIGH + Moderate + Low). **Direct bumps**: `next` 16.1.6 → 16.2.4 (closes 1 HIGH DoS Server Components + 4 Moderate next/image cache + smuggling + CSRF + 1 Low; published 25 days, passes 7-day rule); `next-intl` 4.6.1 → 4.11.0 (2 Moderate prototype pollution + open redirect). **Transitive cleanup via `npm audit fix`** (lockfile-only, no package.json changes): rollup arbitrary file write (HIGH), serialize-javascript RCE + DoS (HIGH + Mod), fast-uri host confusion + path traversal (HIGH × 2), flatted prototype pollution (HIGH), minimatch ReDoS × 4 (HIGH), picomatch ReDoS + method injection (HIGH × 2 + Mod × 2), brace-expansion process hang (Mod), ajv ReDoS (Mod). 198 frontend suites / 2817 tests + tsc clean. **Out of scope (deferred)**: 6 residual alerts (4 Low + 2 Moderate via postcss transitive в next/node_modules) — would require Next.js major downgrade per audit fix --force (breaking); accept residual risk per dev-tooling-only impact.

> **Изменения в 0.128.8:** Security + Polish — gRPC-Go Critical CVE + axios HIGH/MEDIUM cluster + T2-1 a11y closure. Closes 1 Critical + 4 High + 6 Moderate dependabot alerts (11 total) + closes pre-existing v0.128.4 a11y debt. **Bumps**: `google.golang.org/grpc` 1.78.0 → 1.81.0 (CRITICAL — authorization bypass via missing leading slash в `:path`); `axios` 1.13.5 → 1.16.0 (closes prototype pollution gadgets / header injection / NO_PROXY bypass / CRLF injection / authentication bypass / multi-CVE chain). **T2-1 1 TDD pair** — `<th aria-label="select" />` (line 115) replaced с `aria-label={t('disciplineItems.bulkEdit.aria.deleteColumnHeader')}` × 4 locales (ru "Колонка удаления", en "Delete column", fr "Colonne de suppression", ar "عمود الحذف"). 198 frontend suites / 2817 tests + 103 Go packages green post-bumps. **Out of scope**: 12 High remain (Next.js × 4 / minimatch × 4 / picomatch × 2 / fast-uri × 2 / flatted / serialize-javascript / rollup) — most via tooling deps requiring root upgrades (Next.js 15.x → 16.x) planned для v0.128.9.

> **Изменения в 0.128.7:** Frontend bulk-edit accessibility + DOM hardening — закрывает out-of-scope backlog из v0.128.4 reviewer round-2 (BulkEditTable.tsx). 4 TDD pairs (RED→GREEN, 8 commits): **P1** `min={0}` на 14 number inputs (defence-in-depth UX guard против negative entries; backend re-validates ranges server-side, frontend `min` attribute prevents invalid commits via native browser stepper); **P2** `sectionID` prop wired в `data-testid={`bulk-edit-table-${sectionID}`}` (replaces v0.128.4 placeholder `void sectionID` — enables stable query selectors в multi-section page tests когда same item.id appears across sections в test fixtures); **P3** empty-state скрывает `<table>` через `{!isEmpty && (...)}` conditional render (a11y win — empty `<tbody>` was being announced как "table, 0 rows" by screen readers; теперь placeholder paragraph reads naturally; pre-existing 'renders column headers' test reseeded `[sampleItem]`); **P4** ARIA labels на 9 cell controls × 2 contexts (existing rows + pending creates) + delete-toggle = 10 keys под `disciplineItems.bulkEdit.aria.*` × 4 locales (19 `aria-label={t(...)}` call-sites). Reviewer single-pass SHIP **mean 9.33 / min 8** (TDD 10 / DDD 9 / CA 9 / Tests 9 / Cohesion 10 / Hygiene 10 / Security 10 / i18n 9 / A11y 8). 5 bulk-edit suites / 94 tests + 11 curriculum suites / 160 tests green. **Out of scope (deferred)**: T2-1 (`BulkEditTable.tsx:115` hardcoded English `<th aria-label="select" />` survived P4 sweep — pre-existing v0.128.4 debt, не v0.128.7 regression; single TDD pair close в v0.128.8); AR translation domain expert review backlog item.

> **Изменения в 0.128.6:** Dependabot dependency sweep + CI unblock + race-fix backfill. Aggregating 14 dependabot PRs unblocked после v0.128.5 CI/CD Pipeline cleanup. Branch protection требует review approval, dependabot не может self-approve — manual approve sweep + auto-merge per-PR. **14 bumps**: 9 Go backend deps (validator/v10 10.30.2, minio-go/v7 7.1.0, miniredis/v2 2.37.0, lib/pq 1.12.3, gocron/v2 2.21.1, sentry-go/gin 0.46.2, jwt/v5 5.3.1, sentry-go 0.46.2, otelgin 0.68.0) + 3 CI actions (upload-artifact v7, build-push-action v7, setup-buildx-action v4) + 2 Docker base images (golang 1.26-alpine, alpine 3.23). **Pre-flight commits**: `a681f921` exempts dependabot[bot] from PR metadata/description validation gates (unblocked sweep); `c9d3cc90` backfills panel-level integration tests для v0.128.4 race-fix coverage (Submit-during-refetch / Cancel-during-refetch flows). **Sweep mechanics**: 6 of 14 PRs initially DIRTY с `go.sum` conflicts после первого Go dep merge — `@dependabot rebase` × 6 разрешил автоматически (dependabot пересчитал go.sum от свежего main). Pure config + lockfile updates — no behavioural change. **Out of scope (deferred)**: 45 security alerts triage (1 Critical gRPC-Go + 17 High npm transitives — spin v0.128.8 security cleanup release); расширение Backend/Frontend/Documentation CI path filters чтобы dependabot PRs тоже проходили full test gate.

> **Изменения в 0.128.5:** CI/CD Pipeline cleanup — `.github/workflows/ci.yml` rewritten 262 → 96 LoC (-63%). Закрывает chronic-red CI/CD Pipeline status (since project inception). **Diagnosis**: `denied: installation not allowed to Create organization package` — org-level GitHub App permission "Create organization package" не granted к Actions installation на `inf-sys-secretary-methodologist` org; workflow-level `permissions: packages: write` necessary но не sufficient — admin must enable. **Cleanup** — single-developer monolith reality reflected: dropped phantom microservices matrix (10 entries для services/auth, services/user, ..., services/integration — directories never existed; project shipped as monolith); dropped duplicate `frontend-test` job (Frontend CI workflow already covers lint + format + type-check + unit + E2E через Playwright); dropped duplicate `backend-test` job (Backend CI workflow covers); dropped `deploy-staging` / `deploy-production` stub jobs (pure echo placeholders, no actual deployment logic; production deploy mechanism лежит outside GitHub Actions); reshaped `build-and-push` → `frontend-image-build` (dropped matrix, set `push: false` + `load: true` — verifies Dockerfile compiles + dependencies resolve, image discarded post-build). **New ci.yml structure** (2 jobs): security-scan (Trivy CRITICAL+HIGH filesystem scan) + frontend-image-build (Docker build verification on main only). Cross-workflow division: Backend CI / Frontend CI / Database CI / Documentation CI / Security & Quality untouched. Re-enable GHCR push когда org admin grants packages-write + "Create organization package" installation permission. Single config-only commit per project precedent (v0.124.1 / v0.125.1 / v0.126.2 / v0.127.0 cleanup pattern).

> **Изменения в 0.128.4:** Frontend bulk-edit table view UI — Layer 4 of 5 в B1a initiative (последний слой) **закрывает initiative полностью** (5 of 5 layers shipped: Section + DisciplineItem + Review Hardening + Bulk-edit + Frontend). Methodist теперь редактирует все дисциплины раздела в едином `<table>` через POST /api/sections/:sectionID/items/bulk (атомарный commit-or-rollback). Plan `docs/plans/2026-05-09-v0128-section-aggregate.md` ADRs 1-14 + session-locked ADRs 15-18: i18n namespace `curriculum.disciplineItems.bulkEdit.*` (parallel structure к existing curriculum.editDialog.*) / no version в request body (backend repo loads server-side fresh entity) / ControlForm typed enum literal с 4 i18n labels / frozen-state hide buttons. Path B chosen — bulk-edit-as-only-editing-surface, sections seeded externally (Section CRUD UI deferred — admin-internal flow). **Brainstorm Q1-Q5 locked**: Q1 useReducer (не Zustand) — single-page lifecycle, isolated testability; Q2 SWR mutate() reload (не optimistic) — admin power-user flow, divergence risk > responsiveness; Q3 inline per-row 409 conflict banner (не modal) — table уже на экране; Q4 Radix Dialog discard-all confirm (не per-row revert) — scope discipline; Q5 Submit gated by `hasPendingChanges` selector — prevents accidental empty 422. **Architecture (8 TDD pairs + fix-cycle)**: foundation hooks Pair 1-3 (`useSections` SWR list + `useDisciplineItems` SWR list + `fetchDisciplineItem` imperative for post-409 outside-tx refetch + `bulkEditDisciplineItems` mutation с try/catch splitting 409 conflict от propagated axios errors via discriminated `BulkEditResult` union) → pure utilities Pair 4-5 (`pickBulkEditErrorKey` table-driven 11 cases / `bulkEditReducer` 14 actions + 3 selectors с invariant guards) → components Pair 6-7 (`BulkEditTable` presentational с inline cell editing + ControlForm select + canEdit gating / `BulkEditPanel` container с Submit/Cancel/per-row 409 conflict banner) → mount Pair 8 (curriculum/[id]/page.tsx — sections list + nested BulkEditPanel per section) → i18n × 4 Pair 9 (32 keys × 4 locales + JSON-load parity test). **Reviewer round-2 SHIP** mean 8.86 / min 8 после fix-cycle (3 race conditions closed: SUBMIT_CONFLICT keep-flag через new SUBMIT_CONFLICT_REFRESHED action — Submit stays disabled через refetch loop; Cancel button gated by `state.submitting` — DISCARD_ALL не fires mid-refetch; SET_REFRESHED_CONFLICT_ITEM ghost-guard — reducer no-op'ит когда conflict for id more longer tracked; plus 3 Tier 2 quick wins: dead displayedField removed, refetch failure logs к console.error, EDIT_ITEM full-snapshot invariant doc). 198 frontend test suites / 2799 tests green. CurrentVersion=0 semantics consumed: BulkEditPanel issues fresh `GET /api/items/:id` per conflict для accurate value (since under RR isolation re-fetch within failed tx returns same snapshot — backend hint, frontend resolves). **Out-of-scope** (v0.128.5+ frontend hardening backlog): ARIA labels на cell inputs / selects, AR translation accuracy для academia terms (нужен domain expert), `min={0}` на number inputs, empty-state table ARIA, sectionID prop в data-testid, panel-level race-fix integration test, Section CRUD UI (Path B locked — sections seeded externally).

> **Изменения в 0.128.3:** Bulk-edit transactional endpoint — backend Layer 3 of 5 в B1a initiative (Phase 4 row #59 «Bulk-edit РПД»). Closes 3 of 5 sub-releases initiative; v0.128.4 frontend table view UI замыкает. Plan ADRs 10-14 locked в pre-flight `docs/plans/2026-05-09-v0128-section-aggregate.md`. **Endpoint**: `POST /api/sections/:sectionID/items/bulk` — single tx applies combined creates+updates+deletes atomically (commit-or-rollback semantic). **Architecture**: ADR-10 — curriculum-bounded `BulkDisciplineItemsUnitOfWorkPG` в новом `internal/modules/curriculum/infrastructure/persistence/bulk_unit_of_work_pg.go`. Begin opens `db.BeginTx(ctx, opts)` с Repeatable Read isolation per ADR-12 (phantom-prevention для long bulk); returns `bulkTxPG` holding `*sql.Tx` + tx-bound DisciplineItem / Section / Curriculum repos. **DBTX refactor**: 3 PG repos (DisciplineItemRepositoryPG / SectionRepositoryPG / CurriculumRepositoryPG) accept new narrow `DBTX` interface (`ExecContext` / `QueryContext` / `QueryRowContext`) вместо `*sql.DB` — both `*sql.DB` (existing DI) и `*sql.Tx` (bulk-edit path) satisfy. Single source of truth для repo SQL. Mirror sqlc-generated pattern. **Conflict response shape (ADR-12)**: collect-all 409 `{error: "VERSION_CONFLICT", conflicts: [{id, expected_version, current_version}]}`, не short-circuit on first — UI shows entire stale set в one render для merge. **CurrentVersion semantics**: `0` под RR isolation (re-fetch within failed tx returns same snapshot — concurrent-committed version invisible from inside tx). Frontend (v0.128.4) refetches outside tx через `GET /api/items/:id` для accurate value. **Authorize-once optimization**: load curriculum once at tx entry, pass status+createdBy primitives к each item authorize — N items = 2 cross-aggregate queries vs naive N×3. **Audit shape (ADR-13)**: single event per bulk operation `discipline_item.bulk_edited` (success) + `discipline_item.bulk_edit_denied` (failure) с reason ∈ {empty_input / section_not_found / forbidden / not_editable / cross_section / version_conflict / invalid / not_found}; per-op counts (created_count / updated_count / deleted_count). **Single-section invariant (ADR-11)**: sectionID from path, не body. Updates/deletes cross-section detected (item.section_id != path :sectionID) → 422 ErrCrossSectionBulkEdit. Empty bulk → 422 ErrEmptyBulkInput. **Tier 2 absorption (Pair 0, ADR-14)**: BEFORE bulk-edit pairs: `discipline_item_usecases_test.go` (595 LoC) split per action в 6 files; `TestDisciplineItemHandler_List_SectionNotFound` integration test added. **Reviewer round-2 SHIP** mean 8.86 / min 8 после fix-cycle: round-1 8.14/7 dropped due к 8 errcheck violations (defer db.Close() без discard) + dead `ErrBulkSectionDeleted` sentinel; round-2 закрыл оба + Tier 2 (re-fetch under RR meaningless — dropped, CurrentVersion=0; handler defensive guard — added explicit ErrBulkVersionConflict fallback case в mapBulkEditError). 16 commits TDD-discipline (Pair 0 × 3 + Pair 1 × 3 refactor+RED+GREEN + Pairs 2-4 RED→GREEN × 6 + Pair 5 handler backfill + Pair 6 DI + 1 fix-cycle). 32 new tests (9 BulkUoW + 11 BulkEdit usecase + 12 handler integration). golangci-lint --max-same-issues=0 → 0 issues. **Out-of-scope (v0.128.4)**: Frontend table view UI + i18n × 4. **Out-of-scope (future)**: FK CASCADE detection (concurrent section delete race) — admin-internal CRUD low race rate, deferred until profiling shows pattern.

> **Изменения в 0.128.2:** Review hardening patch — закрытие 3 Tier 1 findings из retroactive `superpowers:code-reviewer` round против v0.128.1 commits (mean 8.71 / min 8 — strict gate FIX-CYCLE по mean<9). v0.128.1 reviewer triangulation в момент ship'а был skipped per pragmatic time decision с honest disclaimer; v0.128.2 retroactively closes that disclaimer и hardens release surface ДО v0.128.3 bulk-edit shipping. **B1a 4-release initiative re-shaped к 5-release** (v0.128.0 + v0.128.1 + **v0.128.2 review hardening** + v0.128.3 bulk-edit + v0.128.4 frontend) per ADR-9 amendment в `docs/plans/2026-05-09-v0128-section-aggregate.md`. **Tier 1 #1 — `ListBySection` 404 guard** (Pair 0a RED→GREEN TDD): до v0.128.2 endpoint возвращал `200 []` независимо от того, существует section или нет; клиенты не могли отличить «section существует, нет items» от «section gone». Constructor `NewListDisciplineItemsBySectionUseCase` extended с `sectionLookup` narrow port (mirror к create/update/delete cross-aggregate patterns), Execute теперь loads section first, `ErrSectionNotFound` propagates → handler 404. Bulk-edit v0.128.3 inherit guarantee — без guard transactional commit-or-rollback semantics не могли надёжно distinguish empty target от deleted target. **Tier 1 #2 — Audit reason assertion backfill** (`test:` honest label, не TDD — поведение существовало с v0.128.1, missing pin'ы coverage'а): 5 existing denial-path tests (Create Frozen+Invalid, Update ItemNotFound, Delete NonAuthor+ItemNotFound) backfilled с `audit.events[0].Fields["reason"]` assertions; 4 NEW denial tests added (Update Frozen+NonAuthor+Invalid, Delete Frozen). 19 → 23 DisciplineItem usecase tests. Все 12 audit-emitting denial paths теперь pin canonical `disciplineItemDenialFields` wiring (5 Create + 5 Update + 3 Delete reasons matrix complete). **Tier 1 #3 — Shared `withAuth` test helper** (refactor): Section имел named `withSectionAuth`, DisciplineItem inlined the same anonymous middleware pattern в `setupItemRouter` — DRY violation hides production-middleware-key contract pin (chronicle lesson `feedback_handler_context_key_must_match_middleware`). Created `internal/modules/curriculum/interfaces/http/handlers/handlers_test_helpers_test.go` с shared `withAuth(uid, role)` (mirrors prod auth_middleware.go ключи `user_id` + `role`); refactored Section + DisciplineItem handler tests; minimal scope per reviewer recommendation (other 7 `curriculum_*_handler_test.go` files defer if pattern recurs). **Reviewer triangulation MANDATORY** — round-1 SHIP mean 9.14 / min 9 (TDD 10 / DDD 9 / CA 9 / Tests 9 / Cohesion 9 / Hygiene 9 / Security 9). Tier 2 deferred к v0.128.3 pre-flight: handler-level `TestDisciplineItemHandler_List_SectionNotFound` integration test, `discipline_item_usecases_test.go` split (595 LoC, growing), narrower `Exists` port consideration. 4 commits TDD/backfill/refactor train: `76c2403f` RED + `4251657b` GREEN + `84748ba2` backfill + `0d1a82c6` refactor.

> **Изменения в 0.128.1:** DisciplineItem aggregate (Layer 2 of B1a hierarchy) — Curriculum → Sections → DisciplineItems двухуровневая иерархия завершена backend-side. ControlForm typed VO (4 РФ форм: zachet / exam / course_project / differential_zachet). Rich invariants: 4 hours columns (lectures / practice / lab / self) ≥ 0 each, credits ≥ 0, semester ∈ [1, 12], control_form ∈ enum. 5 CRUD endpoints за RequireNonStudent: POST/GET-list `/api/sections/:sectionID/items`, GET/PUT/DELETE `/api/items/:id`. Migration 035 с 10 CHECK constraints + FK CASCADE. Two-level cross-aggregate lookup в write usecases (section → curriculum для status + author primitives). Audit shape distinct stream `curriculum_section_item`. 5 RED→GREEN TDD pairs (Pairs 1-4 strict, Pair 5 backfill для mechanical handler layer). `AuthorizeDisciplineItemEdit` free function declared from первого draft (chronicles lesson v0.128.0 — avoid Pair 2 → Pair 4 refactor leak); method delegate exists для ergonomic Update/Delete usecase call sites. sqlmock WithArgs pin all 3 Update branches от первого draft (closes mutation-resistance gap class). Reviewer triangulation **skipped** per pragmatic time decision (mirror к v0.128.0 architecture, lessons learned applied — honest disclaimer: should run reviewer pass before v0.128.2). **Out of scope (deferred)**: bulk-edit transactional endpoint → v0.128.2 (closes TOCTOU window via tx serialization); frontend table view UI → v0.128.3.

> **Изменения в 0.128.0:** Section aggregate (раздел учебного плана) — backend foundation для bulk-edit РПД (Phase 4 row #59). Двухуровневая иерархия `Curriculum → Sections → DisciplineItems` per plan `docs/plans/2026-05-09-v0128-section-aggregate.md` (8 ADRs); v0.128.0 ships **только Layer 1 — Section CRUD** (DisciplineItem v0.128.1, bulk-edit v0.128.2, frontend v0.128.3). **ADR-1 Beta** — Section независимый aggregate root, carries только `curriculumID int64` FK, no navigable Curriculum reference (small-aggregates rule для будущей bulk-edit нагрузки). **ADR-2** — lifecycle inheritance, no own status: editability inherits curriculum.status; freeze enforced в use-case layer через `AuthorizeSectionEdit` free function (cross-aggregate primitives — actorID + isAdmin + curStatus + curCreatedBy). **ADR-3** — optimistic locking foundation: `version INT NOT NULL DEFAULT 0` column + UPDATE с `WHERE id = ? AND version = ?` + `version = version + 1`. RowsAffected == 0 disambiguated via follow-up `SELECT 1` — distinguishes stale-version race (409 ErrSectionVersionConflict) от deleted-row (404 ErrSectionNotFound). Migration 034: `curriculum_sections` table с 4 CHECK constraints (defense-in-depth) + FK ON DELETE CASCADE + idx_curriculum_sections_curriculum_id + reuse shared `update_attendance_updated_at` trigger. 5 CRUD usecases (Create / Get / List / Update / Delete) каждый с narrow port + cross-aggregate `curriculumLookup` для Authorize. 5 HTTP endpoints — POST /api/curricula/:curriculumID/sections, GET /api/curricula/:curriculumID/sections, GET /api/sections/:id, PUT /api/sections/:id, DELETE /api/sections/:id (204). Audit shape: `auditSectionResource = "curriculum_section"` distinct stream от curriculum events; canonical denial fields {actor_user_id, section_id, curriculum_id, reason}; success events `section.created` / `section.updated` / `section.deleted`. Reviewer round-1 mean 8.86 / min 8 (FIX-CYCLE single MUST — sqlmock WithArgs missing на 2 of 3 Update branches per CLAUDE.md feedback_sqlmock_withargs_for_mutation_resistance.md), round-2 SHIP mean 9.0 / min 9. **Out of scope, deferred к v0.128.1+**: DisciplineItem entity + migration 035 (Layer 2 — hours/credits/ControlForm), bulk-edit transactional commit-or-rollback endpoint, frontend table view UI.

> **Изменения в 0.127.0:** Pre-commit hook setup — закрывает cumulative cleanup-patch class. 4 patches за месяц (v0.121.3, v0.124.1, v0.125.1, v0.126.2) каждый catches different lint regression после CI feedback. Hook lives at `.husky/pre-commit` repo root (mixed Go + TS), активируется один раз `bash .husky/install.sh` (sets `git config core.hooksPath .husky`). Per-file scope только staged set: Go → AmE/BrE word-boundary grep + `golangci-lint --config=.github/golangci.yml` per-package; frontend (`frontend/src/**/*.{ts,tsx}` flat AND nested + `frontend/messages/**/*.json`) → `prettier --check` + `eslint`. Bypass для WIP — `git commit --no-verify`. Smoke test (`.husky/test.sh`) covers 3/3 violation classes. **Decision: raw shell + `core.hooksPath` over husky framework** для mixed repo (less moving parts, no `frontend/.husky/` conflict с root `.husky/`). Reviewer round-1 8.43/7 → round-2 8.8/8 SHIP. Exception к 3/3 release/session limit (4-й release this session) per user decision — high ROI: eliminates entire lint cleanup-patch class. README updated с install + requirements (bash ≥ 4, golangci-lint в PATH, frontend/node_modules).

> **Изменения в 0.126.3:** Closes v0.126.0 deferred UI seam — admin / methodist users can now flip `methodist_only` flag прямо из `TemplateEditorDialog` (toggle с label + description); до этого нужно было SQL-руками. Pointer semantics across stack: `MethodistOnly *bool` в DTO и repo signature mirroring frontend `methodistOnly: boolean | undefined` state — `nil`/`undefined` = "leave server value as-is", `true`/`false` = explicit set. SQL builder dynamic SET clause (content / variables / methodist_only независимо), `// #nosec G201` justified — все clause entries static literals. 2 RED→GREEN TDD pairs (backend signature extension + frontend toggle UI) + 1 fix-cycle round-1 (prettier blocker + table-driven sqlmock с WithArgs pin per CLAUDE.md ≥3-variant gate). i18n × 4: 2 new keys `templates.editor.methodistOnly{Label,Description}` + JSON-load parity test (mirror к v0.124.0 MFA pattern). Reviewer round-2 SHIP mean 9.0 / min 8 (Tests 10 — mutation-resistant WithArgs sqlmock; Hygiene 9 — prettier clean). Cumulative session shipped 4 releases (v0.126.1 wrong-key + v0.126.2 misspell + v0.126.3 UI). Templates filter теперь end-to-end: backend filter (v0.126.0) + UI toggle (v0.126.3).

> **Изменения в 0.126.2:** Patch — CI lint cleanup. Backend CI на v0.126.0 и v0.126.1 push'ах flagged `misspell` linter (en_US dictionary) на BrE форму `behaviour` в Go комментариях. Fix `behaviour` → `behavior` в 5 файлах: `documents/application/usecases/template_usecase.go:49` + `documents/interfaces/http/handlers/handlers_test_helpers_test.go:26` (pre-existing v0.126.0 carry-over) + `schedule/.../lesson_handler_role_key_test.go:15` + `announcements/.../announcement_handler_role_key_test.go:14, :63` + `users/.../avatar_handler_role_key_test.go:14` (v0.126.1 — added в той же сессии). Pure docs/comments — без behaviour change. 6-й cumulative cleanup patch (mirror v0.124.1 misspell patch). Lesson reinforcement: linter config use en_US dictionary; Russian project context, English Go comments — **must use AmE forms** (behavior / defense / center / color); pre-commit hook backlog для catching до push.

> **Изменения в 0.126.1:** Pre-defence security sweep — closes wrong-key bug class identified в v0.126.0 round-2 reviewer как out-of-scope finding в 4 production handlers. Same shape: handler reads `c.Get("user_role")` но production JWTMiddleware (`auth_middleware.go:59`) writes `c.Set("role", ...)` — role-gated logic silently fails в production despite green unit tests, потому что existing tests mirrored the bug (writing к тому же неверному ключу handler читает). Sites: `schedule/lesson_handler.go:55` (`canModifySchedule` — все schedule write ops silently 403'd для system_admin / academic_secretary), `announcements/announcement_handler.go:47-55` (`isAdmin` — также имел stale value `"admin"` — несуществующая роль; legitimate `"system_admin"` per `auth/domain.RoleSystemAdmin`; admin override на чужих announcements degraded к author-self only), `users/avatar_handler.go:75` + `:200` (Upload + Delete admin override — system_admin не мог управлять чужими аватарами). 3 RED→GREEN TDD pairs + 1 hygiene polish — каждая пара = новый `*_role_key_test.go` файл с production-shaped `withAuth(userID, role)` helper (writes `c.Set("role", ...)` — same key middleware пишет) + GREEN handler fix + cleanup существующих legacy tests которые писали `c.Set("user_role", ...)`. **Reviewer single-pass SHIP**: mean 9.43 / min 9 (TDD 10 / Tests 10 / Security 10 / DDD 9 / CA 9 / Cohesion 9 / Hygiene 9). First single-pass SHIP since v0.122.0. Behaviour change: restoration of intended privilege boundaries, not new privilege grants. **Backlog для v0.126.2**: stale role enum в `custom_report_handler.go:482` + unchecked type assertion в `avatar_handler.go:202`.

> **Изменения в 0.126.0:** Templates filter teacher-own — closes Slot D row #11 backlog item из MAXIMALIST PHASE PLAN. До v0.126.0 `GET /api/templates` возвращал все document templates всем не-студентам — методические templates leak'ались teacher'у. Schema: `methodist_only BOOLEAN NOT NULL DEFAULT FALSE` на `document_types` (migration 033, backwards-compatible default). Domain: `(*DocumentType).CanAccessByRole(role)` failure-closed: open template visible всем; methodist_only template visible только `system_admin / methodist / academic_secretary` (paperwork orchestrators); `teacher / student / unknown / empty role` denied. UseCase `GetAllTemplates(ctx, role)` scope filter. Handler reads `c.Get("role")` (production JWTMiddleware contract) и forwards. Bonus fix — pre-existing `UpdateTemplate` handler bug: тот же неверный context key + stale role values (`'admin'/'secretary'` вместо `'system_admin'/'academic_secretary'/'methodist'`) — production endpoint возвращал 403 всем legitimate caller'ам. Fixed в same release. Test discipline: 3 RED→GREEN pairs + small DTO/frontend type expose + fix-cycle pair (round-1 reviewer caught CRITICAL bug — handler reads wrong context key, filter не работает в production despite green unit tests; round-2 SHIP mean 9.33 / min 9 после adding handler integration tests через production-shaped `withAuth(userID, role)` middleware). Frontend `/documents/templates` page без UI changes — backend filtered list adapts visible items автоматически по роли. **Out-of-scope finding (backlog)**: same wrong-key bug-class identified в schedule / announcements / users avatar handlers — separate fix-pair release.

> **Изменения в 0.125.3:** Polish patch — закрытие optional follow-ups из reviewer round-2 v0.125.2. (1) `refactor(auth)`: extracted `isMFAChallengeActive(): boolean` helper в `frontend/src/stores/authStore.ts`, removing duplication между `LoginForm.onSubmit` + `useLogin.handleLogin` post-await guards; helper вызывает `useAuthStore.getState().mfaIntermediateToken !== null` — imperative read, не subscription (subscription value frozen at render-time, missed value action wrote in same microtask). Render-time gate в LoginForm conditional render остался на subscription (re-render trigger). (2) `test(auth)`: wrapped 3 `useAuthStore.setState({...})` callsites в `LoginForm.test.tsx` (initial seed / mock-login impl / cleanup) в `act(() => {...})` чтобы silence React `"updates were not wrapped in act(...)"` warnings. (3) `docs(auth)`: расширил comment на `pickErrorKey` чтобы document 400 case (binding-layer rejection — backend `auth_handler.go:196-200`; CODE_PATTERN guard prevents в normal use; tampered intermediate → treated as `errorIntermediateInvalid` default-deny). 3 separate commits с honest labeling (refactor / test / docs — НЕ feat). Reviewer single-pass SHIP **mean 9.3 / min 9** (TDD 10 / DDD 9 / CA 9 / Tests 10 / Cohesion 9 / Hygiene 9). Pure refactor + test hygiene + comment — поведение не меняется. Mirror к v0.121.1 / v0.123.1 / v0.124.1 / v0.125.1 single-commit cleanup precedent — пятый cumulative cleanup patch.

> **Изменения в 0.125.2:** Login-flow MFA frontend integration — closes deferred scope из v0.125.0. После v0.125.2 admin с включённой MFA проходит второй фактор полностью через UI: ввод email+password → backend возвращает `mfa_required: true` + `intermediate_token` → LoginForm автоматически переключается на `MFAVerifyLoginStep` (6-значный input mirror'ит MFASettingsCard verify input pattern) → exchange через `POST /api/auth/mfa/verify-login` → access+refresh tokens + redirect. authStore хранит intermediate в ephemeral fields `mfaIntermediateToken` + `mfaPendingUser` (НЕ в `partialize` — не leak'ит в cookie/localStorage; reload теряет challenge, что matches 5-минутному backend window). Status-aware error mapping: 422 INVALID_MFA_CODE → inline localized error + retry; 401 (intermediate dead) или unknown → toast + clearMFAChallenge → возврат на credentials (default-deny). Ghost-success bug закрыт двумя guards: LoginForm.onSubmit + useLogin.handleLogin читают `useAuthStore.getState().mfaIntermediateToken` после `await login()` и skip toast/onSuccess/router.push если challenge active. i18n × 4: 8 keys `loginForm.mfaPrompt.{title, subtitle, codeLabel, submit, errorInvalidCode, errorIntermediateInvalid, resendNote, loginAgain}` × ru/en/fr/ar; JSON-load parity test (pattern v0.124.0) catches drift. 9 commits = 3 RED→GREEN pairs + 1 backfill + 2 fix-cycle pairs (после round-1 reviewer 7.7/10 mean → round-2 9.0/10 mean SHIP). Frontend: 189 suites / 2688 tests green. Backend untouched. **Now MFA полностью end-to-end UI**: enrollment в `/admin/settings/security` (v0.124.0) + login со вторым фактором (v0.125.2).

> **Изменения в 0.125.1:** Patch — `.env.example` sync с новой env var `JWT_MFA_INTERMEDIATE_SECRET` из v0.125.0. Documentation CI на v0.125.0 push flagged var как missing в `.env.example` (CI script verify-env-config-sync проверяет что все `getEnv(...)` calls имеют соответствующие записи). Pure docs sync, без behaviour change.

> **Изменения в 0.125.0:** Login-flow MFA gating (backend). Закрывает deferred scope из v0.124.0 — после verify password backend больше не выдаёт access+refresh tokens пользователю с `MFAEnabled=true`; вместо них возвращает 5-минутный intermediate JWT signed with separate `mfaIntermediateSecret` (env `JWT_MFA_INTERMEDIATE_SECRET`) с claims `{user_id, exp, iat, nbf, jti(uuid), iss=inf-sys-auth-mfa-intermediate, purpose=mfa_verify}`. Frontend handles `mfa_required: true` response → собирает 6-digit code → вызывает новый endpoint `POST /api/auth/mfa/verify-login` (public-rate-limit group, БЕЗ JWT middleware — intermediate IS the auth). VerifyLoginMFA usecase: parse intermediate (with mfaIntermediateSecret) → validate iss/purpose/jti/exp → check jti not revoked → load user via GetByIDForAuth → defence-in-depth check user.MFAEnabled still true → TOTP verify with ±1 step drift → revoke jti (replay protection через existing RevokedTokenRepository, mirror logout pattern) → generate access+refresh tokens. Sentinel errors → status mapping: ErrIntermediateInvalid/Expired/Used → 401, ErrInvalidMFACode/ErrMFANotEnabled → 422. AuthUseCase теперь возвращает `*LoginResult` struct вместо 4-tuple; legacy Login (no-user, agentsim) untouched; setter `WithMFAVerification(revokedRepo, drift, clock)` wires verify-path deps без constructor pollution. Failure-closed: `mfaIntermediateSecret` обязателен в production config (validation rejects placeholder). 4 RED→GREEN TDD pairs strict + 2 refactor commits + 2 fix-ups. Backend: lint 0 / gosec 0 / 103 packages green. **Out of scope (deferred к v0.125.1)**: frontend Login MFA step component + i18n × 4 (8 keys: `login.mfaPrompt.{title,subtitle,codeLabel,submit,errorInvalidCode,errorExpired,errorIntermediateInvalid,resendNote}`). После v0.125.1: full clickable MFA-gated login для system_admin без regressing 4 не-admin ролей (pin via existing 30+ non-MFA test sites).

> **Изменения в 0.124.1:** Patch — CI lint cleanup на v0.124.0 push. Два misspell hits в Go комментариях: `cmd/server/main.go:1379` MFA wiring docstring `defence` → `defense`, `internal/modules/auth/infrastructure/persistence/user_repository.go:118` scanUserByQuery docstring `centralised` → `centralized` (golangci-lint v2.12.2 misspell linter en_US dictionary, в локальный pre-commit scope не входил). Plus prettier drift на `messages/{ar,en,fr,ru}.json` после v0.124.0 i18n keys — `prettier --write` нормализовал все четыре locale файла. После патча `golangci-lint run` 0 issues + `prettier --check messages/*.json` clean. Поведение не изменилось — pure style cleanup.

> **Изменения в 0.124.0:** MFA TOTP enrollment для system_admin. Backend self-implements RFC 6238 (HMAC-SHA1, 30s step, 6-digit truncation per RFC 4226 §5.3, Base32 secret) в `internal/shared/security/totp/` — zero third-party dependencies. Migration 032 добавляет `users.mfa_secret VARCHAR(64)` + `users.mfa_enabled BOOLEAN NOT NULL DEFAULT FALSE` + partial index на enrolled rows. Domain: `MFASecret` VO (32-char Base32, 160-bit per RFC 6238 §5.1, validated в конструкторе) + методы `User.BeginMFAEnrollment / EnableMFA / DisableMFA` с invariants и `var ErrMFAAlreadyEnabled / ErrMFANotEnabled / ErrMFANotPending / ErrInvalidMFACode`. UseCase orchestrates `BeginEnrollment` (generate secret → persist pending → return otpauth URI + secret), `ConfirmEnrollment` (verify ±1-step drift → flip enabled), `Disable` (verify → clear). Audit events `mfa_enrollment_begin/confirm/disabled` через узкий `AuditEmitter` интерфейс (DIP в consumer package). `GetByIDForAuth` добавлен в `UserRepository` (bypass-cache mirror к `GetByEmailForAuth`) — cache wrapper делегирует underlying repo, MFASecret держится с `json:"-"` (не сериализуется в Redis). Handler exposes `POST /api/auth/mfa/{begin,confirm,disable}` за `JWTMiddleware + RequireRole("system_admin")`; status mapping: 200 success / 400 malformed / 401 missing user_id / 409 state conflict / 422 invalid code / 500 opaque. `escapeOTPLabel` percent-encodes `:` в otpauth label (url.PathEscape preserves `:` per RFC 3986 — authenticator apps split on first `:`, нужно %3A). Frontend: `/admin/settings/security` (4-я tab) с `MFASettingsCard` state machine (idle ↔ enrolling ↔ disabling), отображает Base32 секрет + полный otpauth URI как labelled code blocks (QR rendering deferred — supply-chain rule блокирует install QR библиотеки <7 дней; manual entry и URI import оба supported authenticator-приложениями). `User.mfa_enabled` exposed в Login/Register response → frontend reads via `useAuth().user`. i18n × 4 (17 keys) parity verified через JSON-load test, который читает реальные locale файлы (catches namespace bug class hidden by `useTranslations` mock). Backend lint 0 / gosec 0 / 103 packages green; frontend 187 suites / 2668 tests passing. Reviewer SHIP @ mean 9.1/10 / min 8.5 после двух fix-cycle rounds (round-1 6.0 → 8 fixes; round-2 8.4 → 4 fixes). **Out of scope (deferred):** login-flow MFA gating (Login до сих пор issues tokens без проверки второго фактора — отложено в v0.125.x чтобы не trigger регрессию для 4 не-admin ролей перед защитой; audit log already records all enrollment transitions для security-conscious admin); QR rendering (supply-chain block); recovery codes (отдельный migration + use case).

> **Изменения в 0.123.1:** Patch — CI/CD Pipeline frontend-test prettier violations fixes которые local ESLint миссировал (cache miss на specific files): `permissions.ts:131` CURRICULUM_WRITE_ROLES array inline вместо multi-line; `page.test.tsx:186` role union type literal multi-line под print-width. `eslint --fix` auto-applied. No behaviour change.

> **Изменения в 0.123.0:** Curriculum polish bundle (4 reviewer-driven items). (1) `CREATE_ROLES` Set из page.tsx поднят к typed UserRole enum в `frontend/src/lib/auth/permissions.ts` как `CURRICULUM_WRITE_ROLES` + `canWriteCurriculum()` helper (v0.122.0 reviewer follow-up; mirror `EDIT_ROLES`/`canEdit` shape но diverges по составу — academic_secretary + teacher только read). (2) Backfill regression test для CreateCurriculumDialog reset useEffect (pin "type → close → reopen → empty form" behavior, защита от refactor regression). (3) Pagination UI на /curriculum list — Prev/Next buttons inline с count label; offset state на page (limit=20); filter changes reset offset back to 0 via useEffect (narrowing filter на page 3+ не leave on out-of-range page); pagination block внутри items.length > 0 branch. (4) Pending-count badge на /admin/curriculum/approve header — amber chip с total когда > 0, hidden на empty state; pulls `total` из existing useCurricula return (no extra request); renders inline в page header (NavItem infrastructure не trogан — disproportionate). i18n × 4 parity: pagination.{prev,next} + adminApprove.pendingCountLabel ({count} interpolation). 1 backfill test + 2 TDD pairs (5 pagination + 2 badge). Frontend total 185 suites / 2657 tests passing (+8 vs v0.122.0). Out of scope (deferred): full NavItem.badge integration (3 consumers + tests, disproportionate) → возможно v0.123.x или Slot D, URL search-params persistence для pagination → backlog.

> **Изменения в 0.122.0:** Curriculum Create dialog (new draft) — closes the "create new draft" gap. До v0.122.0 учебные планы появлялись только через DB seed или curl backend API; после v0.122.0 methodist + system_admin создают draft через UI на `/curriculum`. CreateCurriculumDialog (Radix modal с 5-field form, mirror к EditCurriculumDialog: client validation = domain invariants verbatim — trim non-empty / year ∈ [2000, 2100] / description ≤ 4096; error mapping by HTTP status 409→codeExists / 422→invalidInput / 403→forbidden / default→generic; dialog stays open on error; useEffect resets all inputs on each open=true). createCurriculum(body) POST helper (thin wrapper around apiClient.post; backend stamps created_by from JWT subject — no client-side actor field). Create button на /curriculum visible только methodist + system_admin (CREATE_ROLES set mirror к backend POST handler write-whitelist v0.116.0; other non-student roles keep read-only list view). Pinned via table-driven tests across all 4 non-student roles. SWR mutate() on success — new curriculum appears в list без hard reload. i18n × 4 parity (16 createDialog.* keys + curriculum.createButton). 13 new tests (2 hook + 8 dialog + 5 page) — 185 suites / 2649 tests / +1 suite / +18 tests vs v0.121.3 baseline. Reviewer SHIP mean **9.67/10** single-pass (TDD 10 / Clean Architecture 9 / Test coverage 9 / i18n parity 10 / Behavior preservation 10 / Commit hygiene 10) — каждая ось ≥9. Out of scope (deferred to v0.122.x): pagination UI на /curriculum list, status pill в navigation badge для admin "3 pending", CREATE_ROLES typed UserRole enum refactor, reset-useEffect regression test.

> **Изменения в 0.121.3:** Patch — Backend CI fully green after reviewer fix-cycle. Independent reviewer #1+#2 на v0.121.2 (mean 6.83/10) flagged три gap'а: (1) mod-verify job был RED из-за uncommitted `go mod tidy` diff (XSAM/otelsql indirect→direct + outdated otel/sys deps в go.sum), pre-existing второй из двух CI failures на v0.121.1 не покрыт patch'ем; (2) errorKey extract применён только в 4 пакетах — оставшиеся 5 handler packages с дубликатами держались под inflated goconst threshold 30; (3) goconst threshold 30 — workaround. Three commits закрывают gap'ы: `chore(deps): go mod tidy` (closes mod-verify red), `refactor(http): extend errorKey + unauthorizedMsg extracts to remaining 5 packages` (announcements + documents + reporting + schedule + tasks — pattern uniform across all 9 handler packages; plus unauthorizedMsg в notifications для 18+ occurrences), `refactor(agentsim,lint): extract fixture agent names + tighten goconst threshold` (`AgentMethodist`/`AgentAcademicSecretary` shared consts; `min-occurrences 30→25` с explicit backlog comment про mid-range log field keys в document/dashboard usecases). После v0.121.3: golangci-lint 0 issues, gosec 0 issues / 49 nosec, 103 packages tests ok, ALL 7 CI workflows green.

> **Изменения в 0.121.2:** Patch — Backend CI flipped red на v0.121.1 push due to golangci-lint version drift (`'latest'` в backend-ci.yml вытащил v2.12.2 при локальной v2.11.4; v2.12+ goconst scans more contexts → 50+ noise issues). Three fixes: (1) pin `GOLANGCI_LINT_VERSION: 'v2.12.2'` (deterministic CI, no future drift), (2) tame goconst в `.github/golangci.yml` (exclude `_test.go` для test fixture repetition + bump min-occurrences 3→30 для filtering natural JSON/log field reuse), (3) extract `const errorKey = "error"` per gin handler package (cmd/server / ai/handlers / integration/http / notifications/http) для closing 11 hits на `gin.H{"error": ...}` идиоме (30-71 occurrences each). После v0.121.2: `golangci-lint run` под v2.12.2 → 0 issues / 103 packages ok / 0 FAIL / gosec 0 issues / 48 nosec.

> **Изменения в 0.121.1:** Patch — `--version` banner синхронизирован с VERSION файлом. До патча `./server --version` выводил `v0.1.0` при VERSION=0.121.0 (pre-existing legacy с момента создания `--version` flag, всплыл во втором независимом ревью v0.121.0 как pre-defence cosmetic risk). Заменили hardcoded literal в `cmd/server/main.go:handleVersionFlag()` на `"v" + versionString` где versionString — package-level const обновляемая `_tools/bump_version.sh` атомарно вместе с остальными 8 version-файлами (extended sed pattern). После патча `go run ./cmd/server --version` выводит корректный `v0.121.1`. No behaviour change кроме корректного вывода версии — lint clean (0), gosec clean (0), все тесты проходят.

> **Изменения в 0.121.0:** Backend lint cleanup sprint — closing 41 lint issues + 6 gosec issues, накопленных через 24 предыдущих релиза. После v0.121.0 `golangci-lint run --config=.github/golangci.yml` reports **0 issues**, `gosec ./...` reports **0 issues**, все 103 backend packages pass tests, **CI Backend + Security & Quality flipped к green** (все 6 workflows зелёные на main). Категории: 24 revive doc-comment additions в `internal/modules/schedule/domain/` (entities + repositories interfaces — exported types, methods, const blocks, sentinel error block); 4 staticcheck/unconvert/errcheck trivial fixes (duplicate `notifDto`/`notifDTO` import unified в cmd/server/main.go, redundant `string(s.RiskLevel)` cast removed, `_ = f.SetSheetName` + `_ = resp.Body.Close` errcheck wraps); 3 goconst test-literal extractions (`testNameAdmin`, `testTitleNew` reuse, `testTime0900`); 1 gocyclo refactor (main() complexity 72→<70 через extraction `handleVersionFlag()` + `initSentry(cfg)`); 5 gosec false-positives annotated `#nosec G101` (4 password-reset UI strings + 1 Redis key namespace prefix); 3 gosec G202 false-positive annotations + 2 real fixes (parameterize LIMIT/OFFSET через `$1/$2` placeholders в reference_repository_pg.go ListStudentGroups + ListDisciplines). Reviewer SHIP single-pass mean **8.6/10** (TDD 9 / DDD 7 / CA 9 / Quality 9 / Hygiene 9). DDD 7/10 — pre-existing baseline (4 schedule repository интерфейса лежат в `domain/repositories/`, по проектному гейту должны быть в `application/usecases/`); спринт нарушение не создал, отмечено в backlog для отдельного architectural-debt sprint. 6 commits non-AI authored. Out of scope: B → v0.121.x curriculum polish (Create dialog + pagination + nav badge), D → 2 quick patches (templates teacher-own / users secretary-groups), repository interfaces relocation (DDD), `cmd/server/main.go:161` `"v0.1.0"` хардкод → ldflags, `sqlmock` unit-tests для reference_repository_pg.go.

> **Изменения в 0.120.0:** Curriculum admin approve queue + Approve/Reject dialogs — **defence-ready минимум закрыт 100%**. `/admin/curriculum/approve` admin-only страница (single-role allowlist; non-admin redirected к /forbidden) с pending_approval queue, status pill, Approve + Reject action buttons per row. ApproveCurriculumDialog (Radix confirmation, mirror к SubmitCurriculumDialog — no form, empty body POST). RejectCurriculumDialog (Radix form mirror к ReturnDialog — reason textarea с client validation trim non-empty + ≤4096 chars + character counter; variant='destructive' confirm button). Хелперы `approveCurriculum(id)` + `rejectCurriculum(id, body)` POST wrappers. RejectCurriculumRequest type re-introduced alongside its consumer. Navigation entry `curriculumApprove` в adminGroup с ClipboardCheck icon, single-role allowlist mirror к backend RequireRole(SystemAdmin) gate. i18n × 4 parity (+37 keys → 117 total per locale). 46 new module tests (16 hook + 7 ApproveDialog + 12 RejectDialog + 17 page + 5 nav); frontend total 184 suites / 2629 tests passing. Reviewer SHIP mean 9.33/10 single-pass + fix-cycle (added nav filter test для non-admin + spinner shell test + conditional dialog render + inline comment). Curriculum end-to-end UI: methodist creates draft → edits → submits → admin approves OR rejects (с reason) → если rejected methodist revises + resubmits. Все 7 backend endpoints (v0.116.0+v0.117.0) имеют UI consumers (v0.118.0+v0.119.0+v0.120.0). Out of scope: create dialog (v0.121.0), pagination UI / status pill badge (v0.121.0), bulk approve, notification UI.

> **Изменения в 0.119.0:** Curriculum detail page + edit dialog + Submit dialog — закрывает methodist self-edit cycle. `/curriculum/[id]` страница со status pill, status-aware action buttons (Edit + Submit visible только status='draft'; pending+approved+archived render read-only metadata + status hint в colored panel). `EditCurriculumDialog` (Radix modal с 5-field form: title/code/specialty/year/description; client validation mirrors domain invariants verbatim — trim non-empty / year ∈ [2000, 2100] / description ≤ 4096; error mapping sentinel-first 409→codeExists / 422→notEditable / 403→forbidden; dialog stays open on error; useEffect resets form state on reopen). `SubmitCurriculumDialog` (Radix confirmation modal, mirror к ResubmitDialog — state transitions consistently use dialogs across codebase). Хелперы `updateCurriculum(id, body)` + `submitCurriculum(id)` POST wrappers. Shared `status.ts` module экспортирует `STATUS_STYLES` (color palette × 4 lifecycle states + lucide icons) + `statusKey()` mapper — single source of truth, used by CurriculumCard + detail page. `UpdateCurriculumRequest` type re-introduced alongside its consumer dialog (CLAUDE.md "никаких на будущее"). i18n × 4: 80 keys/locale (+58 vs v0.118.0). 53 new module tests; frontend total 181 suites / 2583 tests passing. Reviewer SHIP mean 9.43/10 post-fix-cycle (single-pass был 8.67/10 с axis 7 cohesion 6/10 due to duplication; fix-cycle extracted shared module + wrapped Submit в dialog + добавил 2 missing tests + обновил plan ADR-5).

> **Изменения в 0.118.0:** Curriculum frontend list page — `/curriculum` страница для methodist / system_admin / academic_secretary / teacher с filters (status select со 5 опциями, year, specialty), CurriculumCard grid (1-2-3 col responsive) + status pill (color-coded slate=draft / amber=pending / emerald=approved / zinc=archived с lucide иконками), empty state, error block, page-shell guard ДО data-loading branch. Hooks `useCurricula(filter?, opts?)` + `useCurriculum(id, opts?)` поверх SWR с `FetchOpts.enabled` flag (default true) — false short-circuits SWR key к null для skip 401 round-trip когда student briefly authenticated до /forbidden redirect (SEC pattern из v0.114.0). Three-condition fetch gate: `!isLoading && isAuthenticated && user?.role !== 'student'` (стрictly stricter mirror к /my-assignments). Navigation entry `BookMarked` icon в educationGroup, role whitelist mirror к backend `RequireNonStudent` gate. i18n × 4: 22 keys per locale (21 curriculum.* + nav.curriculum) — title / description / loadFailed / countLabel / filters.{status, year, specialty, statusOptions.{all, draft, pending, approved, archived}} / empty / card.{openAria, status.{draft, pending, approved, archived}}; `pending_approval` wire format маппится к UI-shorter `pending` key. Curriculum types layer (Curriculum, CurriculumStatus enum, CurriculumListResponse, CurriculumListFilter) — без forward-looking request DTOs (CLAUDE.md "никаких на будущее"; Create/Update/Reject types лендятся alongside их UI consumers в v0.119.0/v0.120.0). 50 new tests (8 hook + 9 card + 12 page + 21 nav). Frontend total 178 suites / 2530 tests passing (+3 suites / +30 tests vs v0.115.0 baseline). Reviewer SHIP **mean 9.43/10** single-pass + fix-cycle (drop 3 future request types + add countLabel-hidden coverage). Out of scope: detail page `/curriculum/[id]` (v0.119.0), edit dialog + submit button (v0.119.0), admin approve page `/admin/curriculum/approve` (v0.120.0), pagination UI + status pill в navigation badge (v0.121.0).

> **Изменения в 0.117.0:** Curriculum approve workflow — три lifecycle transitions поверх v0.116.0 CRUD замыкают author→approve loop на backend. `SubmitForApproval` (methodist или admin → pending_approval), `Approve` (admin-only → approved + записывает approved_by/at), `Reject` (admin-only → draft, причина в audit per ADR-3, не на entity). Три новых endpoint'а: `POST /api/curriculum/:id/submit` (под curriculumGroup за RequireNonStudent, handler whitelist methodist+admin), `POST /api/curriculum/:id/approve` и `POST /api/curriculum/:id/reject` (под новым adminCurriculumGroup sibling за `RequireRole(SystemAdmin)`, mirror v0.112.0 sibling-route pattern). `Reject` body `{reason}` — handler enforces non-empty trim (400). Audit symmetry: 6 новых событий (`submitted`/`submit_denied`/`approved`/`approve_denied`/`rejected`/`reject_denied`) + transport-skip-audit invariant. `emitAudit` + `denialFields` helpers extracted (N=5 trigger), v0.116.0 callers migrated в same release. Без миграции — migration 031 уже provisioned status/approved_by/approved_at nullable. Reviewer SHIP **mean 10.0/10 every axis** single-pass (TDD 10 / DDD 10 / CA 10 / Security 10 / Tests 10 / Cohesion 10) — второй 10/10 single-pass за curriculum line. Author→Approve loop теперь полностью замкнут на backend: demo flow методист создаёт draft → submits → admin approves OR rejects → (если rejected) методист revises + re-submits. UI consume семи endpoint'ов в v0.118.0+. Out of scope: Discipline child entity (post-defence), Archive transition (v0.122.0+), permanent rejection_reason column (audit-only сейчас), frontend (v0.118.0–v0.121.0), workflow approval #41 заглушка.

> **Изменения в 0.116.0:** Curriculum module backend (basic CRUD) — закрывает последний 🔴 defence-critical gap из PermissionMatrix. Новый bounded context `internal/modules/curriculum/` с full Clean Architecture stack: `Curriculum` aggregate root (учебный план: title / code unique / specialty / year ∈ [2000,2100] / description / status / created_by / approved_by/at) с инвариантами + `CurriculumStatus` typed enum (draft/pending_approval/approved/archived) + `AuthorizeEdit` (status-gate ПЕРЕД ownership; admin override на ownership но не на status freeze) + `UpdateBasics` атомарный content-edit. Persistence — `CurriculumRepositoryPG` поверх `database/sql` с filterClause (status / year / specialty / created_by) + ON unique-violation → `ErrCurriculumCodeExists` (409). 4 use cases (Create / Get / List / Update) с failure-closed nil-repo panic, audit symmetry для каждого denial reason (`invalid` / `code_conflict` / `not_found` / `forbidden` / `not_editable`), transport errors propagate БЕЗ audit (audit log records policy decisions, not infrastructure outages). HTTP layer — 4 endpoints под `/api/curriculum` за `RequireNonStudent`: `POST` (write whitelist methodist+admin), `GET /:id`, `GET ?status=...&year=...&specialty=...&created_by=...&limit=&offset=`, `PUT /:id` (admin override через `isAdmin` flag). Strict boundary parsing — `parsePositiveID` отвергает fractional/zero/negative; `parseListInput` отвергает unknown status literal / out-of-range year / non-positive created_by ДО DB round-trip. Migration 031 — `curricula` table с 7 CHECK constraints mirror domain + `chk_curricula_approved_consistency` (defence-in-depth: status=approved implies approved_by/at populated, ловит direct SQL bypass). Frontend pages `/curriculum`, `/curriculum/:id`, `/admin/curriculum/approve` — deferred к v0.118.0–v0.120.0. Approve workflow (admin-only `ActionApprove`) + `Discipline` child entity — deferred к v0.117.0. Reviewer SHIP **mean 9.4/10** every axis (TDD 10 / DDD 10 / CA 9 / Security 9 / Tests 9 / Migration 10 / Cohesion 9), три полировки после ревью.

> **Изменения в 0.115.0:** Student Resubmit UI — **закрывает academic re-grading loop end-to-end в UI**. На detail page `/my-assignments/[id]` для status='returned' появляется кнопка «Пересдать работу» (visible ONLY когда status='returned' — pending/graded прячут кнопку, чтобы не приглашать гарантированный 409 NOT_RETURNED). Клик открывает `ResubmitDialog` (Radix modal — title + description + confirm/cancel, без textarea: backend resubmit endpoint v0.112.0 принимает empty body). Confirm → `resubmitSubmission(assignmentId)` POST хелпер → `toast.success` + `mutate()` SWR refresh → status pill flips на pending без manual reload + close dialog. Error mapping sentinel-first: 409 NOT_RETURNED → toast «Эта работа уже не в статусе Возвращено», 403 forbidden → toast «Можно пересдавать только свои работы» (defended даже когда unreachable через HTTP — handler hardwires student_id = JWT subject), generic → fallback toast. Dialog stays open on error — student может retry без re-opening (mirrors ReturnDialog UX). Hook surface: `resubmitSubmission(id)` thin POST wrapper рядом с `useMyAssignments` / `useMyAssignment`. i18n × 4: новый `myAssignments.resubmitButton` + `myAssignments.resubmitDialog.*` namespace (10 keys × 4 locales, parity verified). Удалён v0.114.0 `myAssignments.detail.resubmitHint` (button live, hint больше не нужен). Тесты: 11 новых (2 hook helper + 8 ResubmitDialog + 1 page button-visibility); frontend total 175 suites / 2500 tests green (+1 suite / +11 vs v0.114.0). После v0.115.0 demo-flow на защите: teacher grade → returned → student resubmit → re-grade — полностью прокликиваемый в UI, не curl.

> **Изменения в 0.114.0:** Student My Assignments — frontend pages поверх v0.113.0 backend endpoints. `/my-assignments` (list page с status-фильтром tabs: all / pending / graded / returned, grid из `StudentAssignmentCard` с status pills и color-coded secondary line — grade fraction для graded, return-reason snippet для returned), `/my-assignments/[id]` (detail page с assignment metadata header + status-aware panel: pending — amber «Ожидает проверки», graded — emerald с {value}/{max} + feedback block, returned — sky с return_reason + дата + hint что Resubmit button появится в v0.115.0). Обе pages с auth guard: non-student → /forbidden client-side mirror к существующему `/assignments` redirect (backend GET /api/assignments/my за `RequireRole("student")` уже отдаёт 401, client-guard skip useless round-trip). Hooks: `useMyAssignments(status?)`, `useMyAssignment(id)` — SWR conventions match существующих `useAssignments` / `useAssignment` (dedupingInterval=SHORT, revalidateOnFocus=false, null-id short-circuit для detail). New navigation entry `myAssignments` под академической группой только для `UserRole.STUDENT` — sits параллельно с existing `assignments` (teacher/admin), не replaces. i18n × 4: новый top-level `myAssignments.*` namespace + `nav.myAssignments` (ru/en/fr/ar parity verified). Тесты: 25 новых (6 hook + 6 card + 6 list page + 7 detail page); frontend total 174 suites / 2486 tests green. Out of scope для 0.114.0: Resubmit dialog + button — v0.115.0 закроет academic loop end-to-end в UI.

> **Изменения в 0.113.0:** Student-facing read endpoints — backend extension для будущего student dashboard. Два новых endpoint'а в существующей `studentAssignmentsGroup` (за `RequireRole("student")`): `GET /api/assignments/my` (список своих работ — denormalised JOIN `submissions × assignments` где `student_id = JWT.user_id`, опциональный `?status=pending|graded|returned` фильтр) и `GET /api/assignments/:id/my` (детальный view конкретной работы — assignment metadata + submission state в одном round-trip). Read scope = "submissions where I am owner": JWT не несёт group_id, расширять JWT = blast radius (re-issue всех токенов), поэтому group-scoped variant отложен до отдельной initiative с group enrichment. Trade-off: студент видит assignment только после первой grade-попытки (lazy submission creation в SaveGrade) — known UX gap, замкнёт eager-seeding initiative. Domain: `Submission.AuthorizeReader(actorID)` (defence-in-depth mirror к `AuthorizeResubmitter` — same predicate, separate verb для clarity на read use case). View `views.StudentAssignmentView` (denormalised assignment + submission columns — single SQL round-trip). Repo: `SubmissionRepository.ListByStudent(studentID, status?)` — JOIN с assignments, `ORDER BY COALESCE(a.due_date, a.created_at) DESC, a.id DESC` (assignments без due_date сортируются по created_at — стабильный fallback вместо bottom of list). Use cases: `ListMyAssignmentsUseCase` (narrow port `MyAssignmentsRepository`, failure-closed validation на non-positive student id), `GetMyAssignmentDetailUseCase` (load assignment → load submission по pair → AuthorizeReader → buildStudentAssignmentView). Handler: `MyAssignmentsHandler` с `List` + `Detail`, ports defined locally, sentinel-first error mapping (404 / 403 / 500), failure-closed DI panics. Тесты: 4 sqlmock + 5 use case + 8 handler — table-driven где ≥3 cases. Out of scope: **frontend pages** (отдельный minor v0.114.0).

## Содержание

1. [Роли в системе](#роли-в-системе)
2. [Личные vs глобальные настройки](#личные-vs-глобальные-настройки)
3. [Что РАБОТАЕТ полностью](#что-работает-полностью)
4. [Что РАБОТАЕТ частично](#что-работает-частично)
5. [Что НЕ РАБОТАЕТ (заглушки)](#что-не-работает-заглушки)
6. [Сценарии по ролям](#сценарии-по-ролям)
7. [Открытые задачи](#открытые-задачи)

---

## Роли в системе

В коде определено **5 ролей** (`internal/modules/auth/domain/role.go`):

| Роль | Код | Назначение |
|------|-----|------------|
| **Системный администратор** | `system_admin` | Полное управление системой, **все системные настройки и интеграции** |
| **Методист** | `methodist` | Методическое обеспечение учебного процесса (без системных настроек) |
| **Академический секретарь** | `academic_secretary` | Административное сопровождение |
| **Преподаватель** | `teacher` | Реализация образовательного процесса |
| **Студент** | `student` | Участие в образовательном процессе (view-only) |

---

## Личные vs глобальные настройки

С версии 0.102.2 в системе чётко разграничены два уровня настроек.

### Личные настройки — доступны ВСЕМ ролям

Любой авторизованный пользователь, независимо от роли, может настраивать своё рабочее окружение. Эти настройки применяются только к данному пользователю и не влияют на других:

| Настройка | Что делает | Где |
|-----------|------------|-----|
| **Выбор темы оформления** | Переключение между светлой и тёмной темой | `Profile` → Appearance |
| **Подключение каналов уведомлений** | Привязка Telegram, выбор email/WebPush, тестовое уведомление | `Profile` → Notifications |
| **Привязка Telegram** | Получение auth-токена бота, верификация | `Profile` → Notifications → Telegram |
| **Тестовое уведомление** | Проверка доставки по выбранному каналу | при настройке канала |
| **Переключение языка UI (i18n)** | ru/en/fr/ar (RTL для арабского) | `Profile` → Language |
| **Редактирование своего профиля** | имя, контакты, фото | `/profile` |

Это базовая функция, такая же тривиальная как «выйти из системы» — никаких особых прав не требует.

### Глобальные настройки — только `system_admin`

| Настройка | Что делает | Где |
|-----------|------------|-----|
| **Глобальная тема и brand** | Корпоративная цветовая схема, логотип, fav icon — применяется ко ВСЕМ | `/admin/settings/appearance` |
| **Глобальные настройки уведомлений** | SMTP-сервер, push VAPID-ключи, токен Telegram-бота | `/admin/settings/notifications` |
| **Управление n8n workflows** | 3 workflow: уведомления документов, алерты пропусков, напоминания дедлайнов | `/admin/settings/automation` |
| **Интеграция с 1С:Университет** | Настройка соединения, маппинг, синхронизация сотрудников/студентов | `/admin/integration` |
| **Управление пользователями (CRUD)** | Создание/редактирование/удаление, назначение ролей | `/admin/users` |
| **Утверждение учебных планов** | `ActionApprove` — единственная роль с этой привилегией | `/admin/curriculum/approve` |
| **Backup, логи, метрики, алерты** | Эксплуатация системы | `/admin/infra/*` |

**Принцип**: всё, что является системной настройкой и влияет на работу системы для всех пользователей или на её взаимодействие с внешним миром — это исключительно admin.

### Матрица доступа (PermissionMatrix)

| Ресурс | system_admin | methodist | academic_secretary | teacher | student |
|--------|:------------:|:---------:|:------------------:|:-------:|:-------:|
| **users** (CRUD) | full | read limited | read limited | read limited | own update |
| **curriculum** (учебные планы) | full + approve | full | read | read+limited update | read limited |
| **schedule** (расписание) | full | read+limited | full | read | read |
| **assignments** (задания) | full | full+limited | read | full+own | own read+execute |
| **reports** (отчёты) | full | full | full | limited | denied |
| **integration** (1С) | **full** | denied | denied | denied | denied |
| **system_settings** (глобальные) | **full** | denied | denied | denied | denied |
| **personal_settings** (свои) | **own** | **own** | **own** | **own** | **own** |

Уровни: `denied < limited < own < full`.

---

## Что РАБОТАЕТ полностью

Backend + Frontend + API + проверено в use-flow.

| Модуль | Backend (LOC) | Frontend | API |
|--------|:-------------:|:---------|:---:|
| **auth** | 2046 | `/login` (+ MFA verify step для admin'а), `/register`, `/forgot-password`, `/admin/settings/security` (MFA enrollment, system_admin only) | ✅ |
| **users** | 2757 | `/users`, `/profile`, `/users/[id]` | ✅ |
| **documents** | 8392 | `/documents`, `/documents/templates`, `/documents/shared` | ✅ |
| **dashboard** | 859 | `/dashboard` (агрегатор виджетов) | ✅ |
| **notifications** | 5666 | `/notifications`, Telegram, Slack, WebPush, Email | ✅ |
| **messaging** | 3521 | `/messages`, `/messages/[id]` (WebSocket) | ✅ |
| **reporting** | 6628 | `/reports`, `/reports/builder` | ✅ |
| **integration** | 5557 | `/integration` (синк 1С) — **только admin** | ✅ |
| **analytics** | 2430 | `/analytics` (риски студентов, тренды) | ✅ |
| **ai** | 5837 | `/ai` (RAG-чат с цитированием) | ✅ |
| **assignments** (академические задания) | ~3500 | `/assignments` (список + grading), `/assignments/[id]/submissions`, `/my-assignments`, `/my-assignments/[id]` (студенту) | ✅ |
| **curriculum** (учебные планы) | ~2800 | `/curriculum` (список с фильтрами), `/curriculum/[id]` (детали + edit + submit), `/admin/curriculum/approve` (admin queue с Approve/Reject) | ✅ |

### Инфраструктура (всё работает, эксплуатацию ведёт админ)

- **OpenTelemetry tracing** — Tempo + OTEL Collector, корреляция логов с trace_id
- **n8n automation** — 3 workflow: уведомления документов, алерты пропусков, напоминания дедлайнов (управляет admin)
- **Loki** — централизованное логирование через Grafana
- **Grafana Alerting** — 7 алертов в Telegram (CPU, RAM, диск, latency, errors, backup)
- **Web Push** — VAPID + Service Worker
- **Uptime Kuma** — status page (Caddy proxy не настроен)
- **Backup** — PostgreSQL + MinIO в S3 (offsite не подключён)
- **PWA** — Service Worker, offline page
- **i18n** — ru/en/fr/ar (RTL для арабского)

---

## Что РАБОТАЕТ частично

| Модуль | Backend | Frontend | Что отсутствует |
|--------|:-------:|:--------:|-----------------|
| **schedule** (расписание пар) | ✅ events + lessons | ✅ `/schedule` timetable grid + `/calendar` events | Полноценное расписание: CRUD пар, замены, справочники |
| **files** | ✅ 1933 LOC | ❌ | Нет файлового менеджера (только через документы и вложения) |

**Закрыто в недавних релизах:**
- **~~tasks~~** — GH [#200](https://github.com/.../issues/200) в **0.101.0**
- **~~announcements~~** — GH [#202](https://github.com/.../issues/202) в **0.102.0**
- **~~admin-permissions-rebalance~~** — внутреннее изменение в **0.102.1**: интеграция 1С → admin
- **~~personal-settings-clarification~~** — **0.102.2**: личные настройки доступны всем ролям

---

## Что НЕ РАБОТАЕТ (заглушки)

| Модуль | Состояние | GitHub |
|--------|-----------|:------:|
| **workflow** (согласование) | Полностью пустая папка, нет `.go` файлов | [#41](https://github.com/.../issues/41) |
| **Электронная подпись** | Не начато — УКЭП/УНЭП, КриптоПро | [#140](https://github.com/.../issues/140) |
| **Авто-расписание** | Не начато — CSP алгоритм | [#139](https://github.com/.../issues/139) |
| **Внешние календари** | Не начато — Google Calendar, Outlook, iCal | [#40](https://github.com/.../issues/40) |
| **Web Speech API** | Не начато — голосовой ввод/вывод в AI-чате | TM #23 |

---

## Сценарии по ролям

> **Личные настройки опускаются в каждом сценарии** — они одинаковы у всех ролей и описаны выше в [Личные vs глобальные настройки](#личные-vs-глобальные-настройки).

### 🔓 Гость (неавторизованный)

**Доступные страницы:** `/`, `/login`, `/register`, `/forgot-password`, `/reset-password`, `/forbidden`, `/offline`

1. Зарегистрироваться (`/register`):
   - Поля: email, пароль, имя
   - **Выбор роли:** только `student` или `teacher` (whitelist для self-registration)
   - После регистрации — auto-login и редирект на `/dashboard`
2. Войти (`/login`) — JWT в httpOnly cookie + sessions в БД
3. Восстановить пароль (если email-сервис настроен)

**🔐 Защита от privilege escalation (фикс GH #199, 2026-04-25):** глубинная защита в 4 слоях (Domain / Usecase / Handler / Frontend).

---

### 👨‍🎓 Студент (`student`)

**Видит в меню:** Dashboard, Documents (просмотр), Schedule (просмотр), Calendar, **My Assignments** (свои работы), Tasks, Announcements, Messages, AI Assistant, Profile

1. Регистрация → авто-логин
2. **Dashboard** — виджеты: ближайшие события, объявления, непрочитанные сообщения
3. **Documents** — только чтение публичных и доступных документов
4. **Schedule** — **просмотр расписания** своей группы (сетка по дням, фильтр по группе/преподавателю)
5. **Calendar** — свои события, расписание группы
6. **Messages** — WebSocket-чаты
7. **AI Assistant** — RAG с цитированием
8. **Tasks** — просмотр заданий (own read+execute)
9. **Announcements** — просмотр объявлений
10. **Мои работы (полный flow с 0.114.0):** `/my-assignments` (список своих submission'ов с status-фильтром: all / pending / graded / returned) + `/my-assignments/[id]` (детальный view с status-aware panel — оценка/feedback или причина возврата). Backend (0.113.0): GET `/api/assignments/my` + GET `/api/assignments/:id/my` за `RequireRole("student")`.
11. **Resubmit на assignments (полный flow с 0.115.0):** на detail page `/my-assignments/[id]` для status='returned' доступна кнопка «Пересдать работу» → `ResubmitDialog` (confirm/cancel) → `POST /api/assignments/:id/resubmit` → status flips на pending. Backend (0.112.0) ownership invariant `Submission.AuthorizeResubmitter` отклоняет попытку пересдать чужую работу с 403 + audit `assignment.resubmit_denied`.
12. *(Личные настройки — стандартно для всех ролей)*

**Что НЕ может:** создавать/редактировать расписание, создавать документы, отчёты (`denied`), аналитика, управление пользователями, любые системные настройки. **Не может пересдавать чужие работы:** ownership проверяется на entity-уровне через `Submission.AuthorizeResubmitter` — попытка пересдать работу другого студента отклоняется с 403 + audit `assignment.resubmit_denied`.

---

### 👨‍🏫 Преподаватель (`teacher`)

**Видит в меню:** Dashboard, Documents (full), Schedule (просмотр), Calendar, Tasks, **Assignments** (grading), **Curriculum** (read), Announcements, Messages, AI Assistant, Users (limited), Profile

1. Регистрация / создание администратором
2. **Dashboard** — виджеты: задания на проверку, ближайшие пары
3. **Documents** — создание/редактирование своих, шаблоны (read-only), маршруты согласования
4. **Schedule** — **просмотр расписания** своих пар (сетка по дням, фильтр по группе/аудитории)
5. **Calendar** — создание событий, назначение участников
6. **Users** — список студентов своих групп (read limited)
7. **Reports (limited)** — по своим группам, экспорт limited
8. **Assignments (полный grading flow с 0.110.0–0.115.0):** `/assignments` (список своих заданий с фильтрами subject/group_name) + `/assignments/[id]/submissions` (inline grade form per submission row, status-фильтр pending/graded/returned). Может вернуть работу через `ReturnDialog` (с textarea причины ≤ 4096 символов) — `Submission.Return` очищает grade triple, статус submission → returned, audit `assignment.returned` сохраняет previous_grade. Студент пересдаёт, учитель re-grade'ит.
9. **Curriculum (read+limited update с 0.118.0+):** `/curriculum` (список с фильтрами по статусу/году/специальности) + `/curriculum/[id]` (детали с status pill). Read-only для учителя; редактирование закрыто `AuthorizeEdit` гейтом (только методист или admin).
10. **Messages** — групповые чаты со студентами
11. **AI Assistant** — расширенные права на RAG
12. *(Личные настройки — стандартно)*

**Что НЕ может:** создавать/редактировать расписание, видеть отчёты других преподавателей, **редактировать curriculum** (только read), grade'ить чужие assignments (`Assignment.AuthorizeGrader` — только автор), создавать пользователей, любые системные настройки.

---

### 📋 Академический секретарь (`academic_secretary`)

**Видит в меню:** Dashboard, Documents (full + Templates), Analytics group (Reports + Analytics), Schedule, Calendar, Tasks, **Assignments** (read), **Curriculum** (read), Announcements, Messages, AI Assistant, Admin group (Users — read limited), Profile

1. Создание администратором
2. **Dashboard** — административные виджеты
3. **Documents + Templates** — full CRUD, шаблоны (создание/редактирование)
4. **Schedule** — **полное управление расписанием** (создание пар, замены, аудитории)
5. **Reports** — full create/read/export
6. **Analytics** — просмотр аналитики студентов (риски, посещаемость, успеваемость)
7. **Users** — read limited
8. **Calendar** — управление событиями
9. **Assignments (read с 0.110.0):** `/assignments` (список всех заданий, не только своих — caller scope unrestricted) + `/assignments/[id]/submissions` (просмотр работ студентов). Может вернуть работу через `ReturnDialog` (`AuthorizeGrader` принимает 4 non-student роли в read-only сценарии; grading закрыт за teacher's ownership).
10. **Curriculum (read с 0.118.0+):** `/curriculum` (список с фильтрами) + `/curriculum/[id]` (детали с status pill). Read-only — `canWrite` whitelist'ит только methodist + admin.
11. **Messages, AI** — стандартно
12. *(Личные настройки — стандартно)*

**Что НЕ может:** редактировать curriculum (только read), создавать/обновлять учебные планы (только методист или admin), создавать пользователей, подписывать задания, утверждать учебные планы (admin-only), любые системные настройки.

---

### 📚 Методист (`methodist`)

**Видит в меню:** Dashboard, Documents (full + Templates), Analytics group, Schedule, Calendar, Tasks, **Assignments** (read), **Curriculum** (full без approve), Announcements, Messages, AI Assistant, Users (read limited), Profile

1. Создание администратором
2. **Dashboard** — методические виджеты
3. **Documents + Templates** — full CRUD, создание шаблонов документов
4. **Curriculum (полный self-edit cycle с 0.118.0–0.119.0):**
   - `/curriculum` — список всех учебных планов с фильтрами status/year/specialty + цветной status pill (черновик / на утверждении / утверждён / архив)
   - `/curriculum/[id]` — детали с status-aware панелью: для status='draft' доступны кнопки **Редактировать** + **Отправить на утверждение**; для pending/approved/archived — read-only с подсказкой почему
   - **EditCurriculumDialog** (Radix modal с 5 полями: title / code / specialty / year ∈ [2000, 2100] / description ≤ 4096) — client-side валидация зеркальная к domain invariants, error mapping 409→codeExists / 422→notEditable / 403→forbidden, dialog stays open on error для retry
   - **SubmitCurriculumDialog** — confirmation modal для перехода draft → pending_approval. После confirm учебный план уходит на утверждение администратору; редактирование блокируется до решения
   - **Утверждение запрещено** (`ActionApprove` → admin-only). Если admin отклоняет с reason — учебный план возвращается в draft, методист видит причину в audit log + UI feedback, правит и отправляет повторно
5. **Reports + Analytics** — full доступ, экспорт CSV/XLSX
6. **Schedule** — read full + limited update
7. **Assignments (read с 0.110.0):** просмотр всех заданий и работ студентов — caller scope unrestricted для методиста
8. **Users** — read limited
9. **AI Assistant** — расширенные права
10. **Calendar, Messages** — стандартно
11. *(Личные настройки — стандартно)*

**Что НЕ может:**
- Утверждать учебные планы (`ActionApprove` → только admin)
- Управлять расписанием (создавать пары — это секретарь)
- Подписывать ЭП (#140)
- Создавать пользователей
- **Настраивать интеграцию с 1С** — только admin
- **Запускать синхронизацию с 1С**
- **Менять глобальные настройки уведомлений** (SMTP, push, Telegram-бот)
- **Управлять n8n workflows**
- **Менять глобальный brand системы**

> ⚠️ **0.102.1:** ранее методист имел доступ к `/integration` для настройки 1С — передано админу. Методист по-прежнему получает уведомления о результатах синхронизации и видит данные внешних сотрудников/студентов в своих use-flow.

---

### 🛠 Системный администратор (`system_admin`)

**Видит в меню:** ВСЁ — Dashboard, Documents, Analytics, Schedule, Calendar, Tasks, **Assignments**, **Curriculum**, Announcements, Messages, AI Assistant, Users, Integration, **Curriculum approval** (`/admin/curriculum/approve`), Admin Settings, `/admin/*`

1. Создаётся при первом деплое или через миграцию
2. **Dashboard** — полная статистика
3. **Users** — full CRUD пользователей и ролей. Единственный, кто создаёт привилегированные роли
4. **Documents** — full доступ ко всему
5. **Curriculum (полный approve workflow с 0.116.0–0.120.0):**
   - `/curriculum` — список всех учебных планов (тот же view что у методиста + фильтры по статусу)
   - `/curriculum/[id]` — детали с full edit override (`isAdmin` flag → `AuthorizeEdit` пропускает ownership-чек на draft'е любого методиста)
   - **`/admin/curriculum/approve`** — admin-only очередь учебных планов в статусе pending_approval. Single-role allowlist (non-admin redirected → /forbidden). Каждая строка показывает curriculum metadata + status pill + кнопки Approve / Reject:
     - **ApproveCurriculumDialog** — confirmation modal → status=pending_approval → approved + записывает approved_by/at + audit `curriculum.approved`
     - **RejectCurriculumDialog** — Radix form с обязательной textarea причины (≤ 4096 символов, character counter, destructive variant) → status → draft + audit `curriculum.rejected` с reason. Reason **audit-only** (не stored на entity per ADR-3) — методист видит её в audit log и UI feedback
   - **Уникальная привилегия `ActionApprove`** — единственная роль которая может утверждать или отклонять учебные планы
6. **Schedule, Reports, Analytics, Assignments** — full
7. **Integration (1С)** — **полное управление**: настройка соединения, маппинг полей, синхронизация, расписание автосинка (cron), частичный синк, откат при ошибках
8. **Settings/Automation** — управление n8n workflows, запуск тестов вручную
9. **Settings/Appearance** — **глобальная** тема и brand системы (применяется ко всем)
10. **Settings/Notifications** — **глобальные** настройки SMTP, push, Telegram-бота
11. **Admin** (`/admin`) — admin-only роуты
12. **Infrastructure** — backup, логи Loki, алерты Grafana, метрики OTEL, восстановление из backup
13. *(Личные настройки — администратор тоже их использует, как любой другой пользователь — выбирает свою тему, привязывает Telegram. Это не привилегия)*

**Уникальные привилегии (нет ни у кого больше):**
- `ActionApprove` на curriculum
- Создание privileged-ролей
- Все глобальные настройки и интеграции
- Управление n8n
- Управление backup и infrastructure

---

## Открытые задачи

### Из GitHub (open issues)

| # | Заголовок | Приоритет |
|---|-----------|-----------|
| [#196](https://github.com/.../issues/196) | Backend Test Coverage до 90% | high |
| [#41](https://github.com/.../issues/41) | Workflow automation (согласование документов) | (workflow label) |
| [#40](https://github.com/.../issues/40) | Внешние календари (Google, Outlook) | — |
| [#80](https://github.com/.../issues/80) | Анализ рынка | medium |
| [#139](https://github.com/.../issues/139) | Авто-расписание (CSP) | low |
| [#140](https://github.com/.../issues/140) | Электронная подпись | low |

### Из Taskmaster (in-progress / pending)

| ID | Задача | Статус |
|----|--------|--------|
| 1 | Workflow automation | in-progress (15%) |
| 8 | Backend Test Coverage 60% | in-progress (~50%) |
| **24** | **Tasks Module Frontend** | ✅ done 2026-04-25 (релиз 0.101.0) |
| 25 | Schedule Lessons Frontend | **blocked** (нет backend для schedule_lessons) |
| **26** | **Announcements Frontend + Backend Attachments** | ✅ done 2026-04-25 (релиз 0.102.0) |
| 27 | **Files Frontend** | pending medium |
| **28** | **🔐 SECURITY: Self-registration** | ✅ done 2026-04-25 (GH #199) |
| **29** | **Admin permissions rebalance (1С → admin)** | ✅ done 2026-04-26 (релиз 0.102.1) |
| **30** | **Personal settings clarification** | ✅ done 2026-04-26 (релиз 0.102.2) |
| 23 | Web Speech API в AI-чате | pending medium |
| 2 | External calendars | pending medium |
| 5 | Auto schedule | pending low |
| 6 | Electronic signature | pending low |

---

## Краткая сводка

✅ **Готово к продакшну (13 модулей):** auth, users, documents, dashboard, notifications, messaging, reporting, integration *(admin-only)*, analytics, ai, **tasks**, **announcements**, **schedule** *(расписание пар + события)*

⚠️ **Backend без UI:** files

❌ **Не реализовано:** workflow (согласование), электронная подпись, авто-расписание, внешние календари

🔐 **Безопасность:** privilege escalation при регистрации **закрыта** (GH #199). Глубинная защита в 4 слоях.

🛠 **Административное разделение (0.102.1):** все системные настройки и интеграции — только `system_admin`.

⚙️ **Личные настройки (0.102.2):** тема и подключение каналов уведомлений доступны **всем ролям** как стандартная функция профиля. Глобальные параметры (SMTP, brand, 1С, n8n) остаются у admin.

📅 **Расписание пар (0.105.1):** полноценный модуль schedule_lessons — CRUD пар, замены, справочники (аудитории, группы, дисциплины, типы занятий, семестры). Сетка расписания `/schedule` с фильтрами и week-type табами. Доступ: секретарь/admin — полное управление, остальные — просмотр.

📊 **Прогресс:** на 2026-04-26 закрыто 73+ GH issues, ~545+ коммитов. Code review compliance: все недавние релизы (0.100.1, 0.101.0, 0.102.0, 0.102.1, 0.102.2) с оценкой ≥9/10 по TDD, DDD, CA.
