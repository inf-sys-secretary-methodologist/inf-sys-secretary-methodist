# v1.0.0 Batch 1 audit aggregation — 2026-05-20

## Reviewer verdicts

| Module | TDD | DDD | CA | Security | Tests | Quality | Mean | Min | Status |
|--------|-----|-----|----|---------|-------|---------|------|-----|--------|
| documents | 7 | 6 | 6 | 6 | 8 | 7 | 6.7 | 6 | FIX-CYCLE |
| curriculum | 9.5 | 7.5 | 7.5 | 8 | 8.5 | 9 | 8.33 | 7.5 | FIX-CYCLE |
| reporting | 3 | 5 | 4 | 3 | 7 | 5 | 4.5 | 3 | **BLOCK** |
| reports/annual | 9 | 9 | 9 | 8 | 9 | 9 | 8.83 | 8 | SHIP standalone |
| ai | 4 | 5 | 5 | 5 | 6 | 6 | 5.2 | 4 | FIX-CYCLE |

## Tier 1 security blockers (exploitable surfaces)

### reporting — BLOCK
1. **SQL injection** — `infrastructure/query/dynamic_query_builder.go:170` interpolates user-supplied `field.Alias` (JSON input from `CreateCustomReportInput.Fields[].Alias`) into `AS %s` без sanitization. Endpoint `POST /api/custom-reports/:id/execute`. `#nosec G201` annotation factually wrong про alias path.
2. **No role gate** — `cmd/server/main.go:2059-2084` mounts `/api/custom-reports` без `RequireRole(...)`. Students могут create+execute. Combined с #1 = authenticated-user SQL injection on production tables.
3. **Production-shipped fake generation** — `application/usecases/report_usecase.go:343-369` `simulateGeneration` спит 2s, hardcodes sample data, `FileSize=0`, lies `mime_type=application/pdf` для несуществующего файла. Either 501 или wire real generator (extract Annual's renderer).
4. **Background goroutines с `context.Background()`** — `report_usecase.go:335,487` leak на shutdown. Violates CLAUDE.md gate.
5. **Pagination + access filter client-side after DB fetch** — `custom_report_usecase.go:171-219` breaks correctness (TotalCount ≠ accessible).
6. **Repository interfaces в `domain/`** — все 3 (`ReportRepository`, `ReportTypeRepository`, `CustomReportRepository`). Violates DIP rule. Annual got it right.
7. **God interface (20 methods)** — `ReportRepository` ISP violation. Split на narrow ports.
8. **`time.Now()` + `uuid.New()` в domain constructor** — `entities/report.go:60-71`, `entities/custom_report.go:165`.

### ai — FIX-CYCLE
1. **`CreateConversation` BROKEN** — `interfaces/http/handlers/ai_handler.go:253-281` ignores `req.Title`, returns user's first existing conversation as "newly created"; fallback returns unpersisted DTO. Никогда не calls `conversationRepo.Create`.
2. **`validate:` tags не enforced** — `application/dto/ai_dto.go:68,73,90,114`. Gin reads `binding:` not `validate:`. Max=10000 на Content игнорируется → token-cost DoS + prompt injection surface.
3. **AI routes без per-user rate limit** — `cmd/server/main.go:1167-1173` mounts `aiAPIGroup` с JWT only, no `publicRateLimiter` / `authRateLimiter`. Flood `/api/ai/chat`.
4. **Schedulers не registered со shutdown** — `factScheduler` + `indexingScheduler` `Start()` invoked, `Stop()` never registered. Both `deliverDailyFact` (`fact_scheduler.go:73`) + `indexPendingDocuments` (`indexing_scheduler.go:65`) hardcode `context.Background()`.
5. **Cross-module imports** — `mood_usecase.go:13-15,24,32` imports `dashboard/application/usecases` + `analytics/domain/repositories`. Также `services/telegram_personality_service.go:9`, `scheduler/fact_scheduler.go:15`. Violates CLAUDE.md gate.
6. **`userID.(int64)` panic** на `ai_handler.go:373,413` — unchecked type assertion.
7. **Ownership errors → HTTP 500** + raw `err.Error()` exposes PG errors (info disclosure) — `chat_usecase.go:97,246,441,453,471,499`.
8. **`openai_provider.go:21`** — `defaultOpenAIChatModel = "gemini-2.5-flash"` — Gemini model хардкод как default для OpenAI provider.

### documents — FIX-CYCLE
1. **Sentinel error mismatch** — `infrastructure/persistence/document_repository_pg.go:129,178,209,499,534,601,617,628,902` returns `fmt.Errorf("document not found")` вместо `usecases.ErrDocumentNotFound`. `errors.Is` false → workflow audit not_found branches dead → `workflow_handler.go:469 mapWorkflowError` fallthrough к 500 instead of 404.
2. **Clickjacking via `?inline=true`** — `document_handler.go:408-419` strips X-Frame-Options + rewrites CSP `frame-ancestors *` для ANY authenticated download.
3. **Header injection** — `document_handler.go:410,418` `fileInfo.FileName` напрямую в Content-Disposition. No CRLF strip / RFC 5987 encoding.
4. **Magic string `"admin"`** — `document_repository_pg.go:255,932`. Project enum = `"system_admin"`. SystemAdmin не bypasses access-control WHERE.
5. **Cross-module import** — `sharing_usecase.go:12,116-125` imports `notifications/application/usecases` + fire-and-forget с `context.Background()` + UI strings в usecase.
6. **`UploadFile` bypasses Register domain method** — `document_usecase.go:306-308` direct mutation `doc.Status = Registered` leaves audit fields empty.
7. **Russian UI strings в usecases** — `tag_usecase.go:171,177,213,240`, `sharing_usecase.go:121-122`, `document_handler.go:231`.

### curriculum — FIX-CYCLE
1. **Repository interfaces в `domain/repositories/`** — `curriculum_repository.go`, `section_repository.go`, `discipline_item_repository.go`, `bulk_unit_of_work.go`. DIP rule violation. Use-case-level narrow ports (e.g., `audit_sink.go`) done правильно.
2. **`curricula` table без optimistic lock** — migration 031 не имеет `version` column. `Update` не WHERE version=?. Section + DisciplineItem правильно используют optimistic lock — aggregate root уязвим к lost-update race.

## Strengths

- **B4 Annual sub-module** (`reports/annual/`) — narrow ports declared в consumer (`annual_report_usecase.go:15-32`); deterministic byte-equality DOCX golden test; explicit XML-injection escape test; httptest integration test через real router; pristine RED→GREEN commit pairs.
- **Documents workflow pack** (v0.148.0+) — AuditSink narrow port + fixed-clock injection + sentinel errors via `errors.Is` + table-driven usecase tests с `fakeWorkflowRepo` + integration через real `gin.Engine` + admin gate + `readActor` helper centralizing context-key reads.
- **Curriculum domain encapsulation** — 100% domain coverage, ReconstituteX pattern, ControlForm typed enum mirrors SQL CHECK byte-for-byte; AuthorizeSectionEdit/AuthorizeDisciplineItemEdit free functions от первого RED draft; TDD discipline impeccable на git-уровне.
- **AI provider abstraction** — `domain/ports/providers.go` narrow LLMProvider/EmbeddingProvider; `FallbackLLMProvider` decorates cleanly; SQL hygiene в `embedding_repository_pg.go` (pq.Array, pgvector.NewVector, parameterized hybrid search).
- **Curriculum security baseline** — parameterized SQL, ORDER BY hardcoded, failure-closed DI panics, defense-in-depth role gates, `actorID > 0` JWT-loss guard, RR isolation for bulk-edit.

## Decisions pending

### Strategy options для batch 1 fixes

**Option A: Lock down vulnerable surfaces (minimal scope для defence demo)**
- Disable `/api/custom-reports/*` + `/api/reports/*/generate` behind feature flag OFF in demo
- Disable `/api/ai/chat` или sharply rate-limit (1 req/min) for demo
- ~1 session (3-4 hours)
- Cons: shipping known broken code; bad for diploma quality narrative

**Option B: Fix Tier 1 security hotfixes only**
- v0.154.0 reporting security (SQL inj + role gate + remove simulateGeneration → 501)
- v0.155.0 ai security (CreateConversation + validate tags + rate limiter + schedulers shutdown + Gemini hardcode)
- v0.156.0 documents security (clickjacking + header injection + sentinel mismatch)
- v0.157.0 curriculum hardening (repo interfaces relocation + curricula optimistic lock)
- ~4-6 sessions (10-15 hours)
- Cons: defers Tier 2/3 cleanup к post-v1.0.0

**Option C: Full Tier 1 + Tier 2 per module before next batch**
- Same as B + absorb Tier 2 в same release commits
- ~6-9 sessions (15-22 hours)
- Cons: longest path

**Option D: Continue с batches 2/3/4 reviews in parallel, fix all Tier 1 collectively before v1.0.0**
- Run remaining 13 modules through reviewer first (3 sessions × 1.5-3 hr = 4.5-9 hr)
- Then comprehensive Tier 1 fix sprint
- ~10-15 sessions total
- Pro: full picture before commit fixes; possibly group cross-module patterns

### Известные deferred (Tier 3, post-v1.0.0 OK)
- AI: `document_adapter.go` DIP refactor; `deliverDailyFact` + `indexPendingDocuments` coverage
- Documents: legacy usecase narrow-port refactor (template, version, tag, sharing)
- Reporting Tier 3 nice-to-have (PDF Cyrillic font, golden tests, N+1)
