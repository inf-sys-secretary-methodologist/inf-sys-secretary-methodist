'use client'

// Pair-4 RED stub — page component shell that satisfies the import
// in __tests__/page.test.tsx but renders the minimum so the
// happy-path / role-gate / metrics / file-table assertions all
// fail until the GREEN follow-up wires the real layout.
export default function AdminBackupsPage() {
  return <div data-testid="backups-page-stub" />
}
