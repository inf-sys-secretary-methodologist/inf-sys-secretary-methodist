'use client'

interface MFASettingsCardProps {
  mfaEnabled: boolean
  onChange?: (enabled: boolean) => void
}

// RED stub — implementation lands in the GREEN follow-up commit.
export function MFASettingsCard(_props: MFASettingsCardProps) {
  return null
}
