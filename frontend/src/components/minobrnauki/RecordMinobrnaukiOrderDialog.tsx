'use client'

// STUB (RED state). Real record form lands in the GREEN commit.
interface RecordMinobrnaukiOrderDialogProps {
  open: boolean
  onClose: () => void
  onCreated?: () => void
}

export function RecordMinobrnaukiOrderDialog(_props: RecordMinobrnaukiOrderDialogProps) {
  return null
}
